# Auditoría de frontera 2026-07-14 (tercera del ciclo)

Frontier-audit multiagente #3, HEAD `0a6f52450` (tras cerrar 6/8 del audit 2026-07-13b + los 4 re-ciclos). **19 frentes, 214 agentes, 92 candidatos → 81 DOBLE-CONFIRMADOS (refuter + matemático independiente, sympy + sustitución + grid), 14 familias.** CERO regresiones de los ~40 fixes previos (los 4 hallazgos del frente de regresión son SIBLINGS adyacentes: el borde c=1 de recíproco-cuadrado y la escritura `1/tan²` de cot²).

Sigue a [AUDITORIA_FRONTERA_2026-07-13b.md](AUDITORIA_FRONTERA_2026-07-13b.md). Metodología idéntica: scouts read-only por frente (DEEP corners, el engine ya tiene ~40 fixes) → dedup → 2 lentes adversariales independientes (doble-CONFIRMED) → síntesis por familia → root-cause read-only con file:line. Los dos lentes advertidos de que sympy solveset MIENTE en abs anidado/trig (sustitución es el árbitro).

## Checklist de cierre (vivo)

- [ ] **F1 [P0]** matrix-power-symbolic-blowup (`[[1,1],[1,0]]^14`) — bounded-single-cycle
- [ ] **F2 [P1]** trig-reciprocal-power-family-drop (`solve(sec(x)^3=8, x)`) — multi-cycle
- [ ] **F3 [P1]** taylor-removable-singularity-undefined (`taylor(sin(x)/x, x, 0, 4)`) — bounded-single-cycle
- [ ] **F4 [P0]** transcendental-inequality-periodicity-drop (`solve(sec(x)^2>2, x)`) — multi-cycle
- [ ] **F5 [P0]** nested-abs-region-enumeration-drop (`solve(abs(abs(x)-abs(x-2))=1, x)`) — multi-cycle
- [ ] **F6 [P0]** modular-exponentiation-missing (`mod(123456789^987654321, 1000000007)`) — bounded-single-cycle
- [ ] **F7 [P0]** abs-inequality-symbolic-center (`solve(abs(x-a) <= 3, x)`) — bounded-single-cycle
- [ ] **F8 [P0]** leibniz-diff-under-integral (`diff(integrate(x*sin(t^2), t, 0, 1), x)`) — bounded-single-cycle
- [ ] **F9 [P0]** product-inequality-symbolic-root-ordering (`solve((x-1)*(x-a) > 0, x)`) — multi-cycle
- [ ] **F10 [P1]** partial-function-domain-constraint-dropped (`solve(sqrt(a-x)=x, x)`) — multi-cycle
- [ ] **F11 [P0]** simplifier-expand-factor-oscillation-hang (`1/(1+2^(1/3)+4^(1/3))`) — multi-cycle
- [ ] **F12 [P2]** complex-value-domain-non-real-dropped (`solve(x^2 + x + 1 = 0, x)`) — multi-cycle
- [ ] **F13 [P1]** bigint-power-eager-materialization (`10^50000000`) — bounded-single-cycle
- [ ] **F14 [P1]** rational-surd-denominator-root-extraction (`solve((x^3-2*x)/(x-sqrt(2))=0, x)`) — bounded-single-cycle

## Cobertura

**4 de 19 frentes LIMPIOS bajo probing PROFUNDO**: `integrals-indef`, `integrals-def`, `limits`, `piecewise-special` — el core-valor de cálculo (antiderivadas, límites, integrales definidas) AGUANTA incluso en corners profundos (Weierstrass, singularidades interiores, L'Hôpital anidado). **Frente de regresión: CERO regresiones** de los ~40 fixes (los 4 hallazgos son siblings no cubiertos).
- Con hallazgos: inequalities (8), abs (8), matrices (8), transcendental-mixed (8), trig (7), derivatives (7), complex (7), exp-log (6), series (6), number-theory (5), radicals (2), identities-simplify (3), solve-poly (1), systems (1).
- Lectura: la frontera se movió a **TRES bandas** — (1) álgebra de solve con PARÁMETROS simbólicos (ineq que ordena raíces indecidibles, abs con centro simbólico, dominio de función parcial dropeado en el path simbólico); (2) completitud de familias periódicas en NUEVAS escrituras (sec/csc/cot^n, recíproco-cuadrado en INECUACIONES, abs anidado); (3) **blowup de evaluación eager** — potencias de matriz, mod-exp, potencias bigint, racionalización surd — que CUELGAN o emiten árboles multi-MB por falta de folding numérico/algoritmos rápidos/orquestación cycle-safe.

## Síntesis

After the periodic-trig-EQUATION and symbolic-linear-inequality rounds landed, the frontier has moved decisively off the definite/real-domain value core (still holding) and onto three bands. (1) The ALGEBRA of solve with symbolic parameters: inequalities that commit to an undecidable root ordering, abs inequalities with a symbolic center, and radical/inverse-trig equations that drop a partial function's implicit domain/range constraint on the symbolic path while the numeric path applies it. (2) The periodic-family completeness discipline, now recurring in new spellings — reciprocal/power trig EQUATIONS collapsing to a principal representative, plus their reciprocal-square INEQUALITY analogs collapsing to ∅/ℝ/one ray, and nested-abs equations dropping half their roots. (3) A broad eager-evaluation blowup band — symbolic matrix powers, huge integer powers, missing modular exponentiation, cubic-surd rationalization, and repeated differentiation — that hangs or emits multi-MB garbage trees for want of numeric folding, fast algorithms, and cycle-safe orchestration. Two clean capability gaps (Taylor through removable singularities; Leibniz differentiation under the integral sign) and the still-cosmetic complex value domain round it out. Highest leverage is the eager-evaluation blowups (bounded, single-fix, P0 hangs) and the two big solve-completeness clusters (trig-family-drop, nested-abs).

## Meta-temas (palanca compartida)

1. **Banda de evaluación eager (P0 hangs, bounded, máxima ROI)**: potencias de matriz simbólica (F1), mod-exp ausente (F6), potencias bigint (F13), y el hang C5 de racionalización surd (F11) comparten la raíz: **falta folding numérico eager + algoritmos rápidos (fast-modexp, matmul numérico) + intercepción raw-tree pre-hijo**. El simplificador bottom-up materializa el hijo `Pow(a,b)` ANTES de que la regla del padre (`mod`) corra.
2. **Parámetro simbólico rompe el ensamblador de intervalos**: abs con centro simbólico (F7), producto con raíces simbólicas (F9), dominio de función parcial (F10) — todos caen al set-algebra value-blind (`compare_values` structural fallback, solution_set.rs:105) que ordena mal endpoints simbólicos → ∅/ℝ/dos-puntos falsos. El patrón `try_solve_abs_vs_symbolic_param` (construcción DIRECTA en la capa solver, bypass del set-algebra) es el playbook.
3. **Completitud de familias periódicas en escrituras nuevas**: sec/csc/cot^n (F2), recíproco-cuadrado INEQUALITY (F4), `1/tan²`=cot² (spelling), abs anidado (F5) — el fix de la ecuación recíproco-cuadrado (`9e4e854aa`) y cot² (`0a6f52450`) son plantillas directas a espejar en el path de inecuación y en las escrituras Laurent.


### F1 [P0] matrix-power-symbolic-blowup

- **Representante:** `[[1,1],[1,0]]^14`
- **Correcto:** [[610, 377], [377, 233]]
- **Miembros (8):** `[[1,1],[1,0]]^14`; `[[2,0,0],[0,1,0],[0,0,1]]^30`; `[[2,0],[0,3]]^20`; `[[1,2],[3,4]]^15`; `[[1,1,0],[0,1,1],[0,0,1]]^10`; `[[1,1],[1,0]]^-14`; `trace([[1,1],[1,0]]^30)`; `det([[1,2],[3,4]]^20)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_math/src/matrix.rs:111 (Matrix::multiply; the dot-product accumulation at lines 129-130 and the mul2_raw helper at line 12)
- **Mecanismo:** `try_matrix_power_expr` (crates/cas_engine/src/matrix_rule_support.rs:382-415) computes M^n for an all-numeric square base by NAIVE repeated multiplication: `result = result.multiply(&base, ctx)` looped `1..n` times (line 410-413). `Matrix::multiply` (matrix.rs:111) builds every product entry as raw SYMBOLIC nodes — each element product via `mul2_raw` = `ctx.add_raw(Expr::Mul(a,b))` (line 12/129) and the dot-product sum via `ctx.add(Expr::Add(sum, product))` (line 130). Neither `ctx.add` nor `add_raw` constant-folds numeric operands (`ctx.add` only flattens/sorts Add/Mul; verified against the emitted `0 + 0 + 1`, `·0` litter). So each matmul feeds the previous UN-folded entry trees back in, and the per-entry expression tree grows in depth every iteration. After ~13 iterations (n=14) the entries are multi-KB/MB uncombined `0/1` arithmetic trees; the simplifier then hits depth_overflow at depth 51 (WARN phase=Core) and either hangs or dumps the garbage tree instead of `[[610,377],[377,233]]`. Every family member routes through this same loop: diagonal/upper-triangular powers, `^-14` (whose `Matrix::inverse` at matrix.rs:702 already folds to rationals, then blows up in the multiply loop), and `trace(...)`/`det(...)` which wrap the same M^n result.
- **Boceto de fix:** see fix_sketch field
- **Blast radius:** Very small. `Matrix::multiply` has exactly two production callers (rg-confirmed): `try_eval_matrix_mul_expr` (single matmul) and `try_matrix_power_expr` (the power loop). Folding only changes the INTERMEDIATE node structure of numeric products; the final simplified answer is identical (the simplifier already folds these to the same Numbers, just without overflowing). The single-matmul CLI test (crates/cas_cli/tests/matrix_operations_tests.rs:88-90) asserts only the final result contains \"19\", not any unfolded `1·5+2·7` form, and `--steps on` for matmul emits no dot-product narration (verified: it prints just `[[19,22],[43,50]]`), so no didactic step-trace regression. Symbolic matmul (`[[a,b],[c,d]]*[[e,f],[g,h]]`) is untouched since its operands don't reduce to rationals. Only edge note: the gate at matrix_rule_support.rs:396 also admits `Expr::Constant(_)` entries (e.g. π), which `as_rational_const` won't fold — but no family member uses those and they fall back to the current (symbolic) path, so behavior there is unchanged, not worsened.
- **Maquinaria reutilizable:** `cas_math::numeric_eval::as_rational_const` (numeric_eval.rs:23) already extracts an exact BigRational from a numeric Expr and is a sibling module of matrix.rs in the same `cas_math` crate. `Expr::Number(BigRational)` is the folded-value constructor (matrix.rs already imports `cas_ast::{Context, Expr, ExprId}`). `Matrix::inverse` (matrix.rs:662-708) already demonstrates the exact-rational fold pattern (`ctx.add(Expr::Number(num/den))` at line 702) — mirror it in `multiply`. The all-numeric gate and MATRIX_POWER_MAX_EXPONENT=64 bound already exist in `try_matrix_power_expr`.
- **ROI:** Matrix power via naive repeated SYMBOLIC matmul that never constant-folds integer entries; the simplifier hits depth_overflow (depth 51) and either hangs or dumps multi-MB uncombined 0/1 arithmetic trees. One well-scoped fix — eagerly evaluate numeric matrix-product entries and use fast (square-and-multiply) exponentiation — closes all 8, including trace()/det() of powers, with no coupling to the symbolic solver. 8 P0 hang/garbage members on trivially common inputs: highest ROI.

### F2 [P1] trig-reciprocal-power-family-drop

- **Representante:** `solve(sec(x)^3=8, x)`
- **Correcto:** x = ±π/3 + 2πk, k ∈ ℤ  (i.e. {π/3 + 2πk} ∪ {5π/3 + 2πk})
- **Miembros (12):** `solve(sec(x)^3=8, x)`; `solve(csc(x)^3=1, x)`; `solve(sec(x)^3=-8, x)`; `solve(sec(x)^5=32, x)`; `solve(sec(x)^3=2*sqrt(2), x)`; `solve(csc(x)^3=8, x)`; `solve(sec(x)^2=1, x)`; `solve(1/cos(x)^2=1, x)`; `solve(1/sin(x)^2=1, x)`; `solve(cot(x)+cot(2*x)=0, x)`; `solve(1/tan(x)^2=2, x)`; `solve(1/tan(x)^2-1=0, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:3038 (solve_polynomial_in_trig_from_diff; via its call to solve_polynomial_in_atom at :2173)
- **Mecanismo:** Reciprocal-power trig equations `sec/csc(g)^n = c` simplify to `1/cos(g)^n` / `1/sin(g)^n`. try_solve_polynomial_in_trig (:3168) substitutes u=cos(g), producing a Laurent polynomial `1/u^n - c`. solve_polynomial_in_atom (:2165) calls `Polynomial::from_expr(...).ok()?` (:2173), which errors on the negative exponent -> returns None. No family-emitting handler catches the reciprocal form, so the equation falls through to the generic unary-inverse isolation, which applies arccos/arcsin (function_inverse.rs:179) and returns only the single principal value (`{π/3}`), dropping the ± companion branch AND the whole +2πk family. The base solvers already emit the full family: `cos(x)=1/2`, `cos(x)^3=1/8`, `sin(x)^3=1/8` all return the correct Periodic set (verified) — only the reciprocal/ratio reduction is missing. An EXACT analog already exists for exponentials — try_solve_exponential_reciprocal_polynomial (:3568) clears `1/u^k` Laurent denominators for `e^x+e^(-x)=2`; there is simply no trig counterpart. Sub-shape (b) (`1/tan(x)^2`) simplifies not to a single-atom reciprocal but to a ratio of TWO distinct atoms `cos(g)^n/sin(g)^n` (a different, harder shape); it currently leaks the lossy residual `solve(cos(x)-sin(x)·sqrt(2)=0,x)` from squaring keeping only the +sqrt branch. `cot(x)+cot(2x)=0` is separate again: a SUM of cotangents whose 2x multiple angle triggers a simplifier depth_overflow (depth=51) and never reaches a clean rational-in-trig form.
- **Boceto de fix:** Mirror the existing exponential Laurent handler for trig. In solve_polynomial_in_trig_from_diff (:3038), after substituting u=trig(g), detect a Laurent u_expr (minimum exponent of u < 0) and multiply through by u^(-min) to clear denominators, yielding a genuine polynomial, THEN hand it to solve_polynomial_in_atom (which back-substitutes each root through the periodic base solver -> full family). No spurious u=0 root can arise (the reciprocal's constant coefficient is nonzero), and `cos^n=1/k>0` never lands on the cos=0 pole, so it is sound. This closes sub-shape (a): sec^3=8, csc^3=1, sec^3=-8, sec^5=32, sec^3=2√2, csc^3=8, sec^2=1, 1/cos^2=1, 1/sin^2=1 (9/12). Gate on min-exponent<0 exactly like try_solve_exponential_reciprocal_polynomial so pure-polynomial trig (already correct) is untouched. Sub-shape (b) [1/tan^2=2, 1/tan^2-1=0] needs a second related step: combine the diff into a single fraction and solve the numerator = 0 when the denominator is a product of trig-atom powers (`cos²/sin² - 2 -> cos² - 2sin² = 0`), which try_solve_mixed_trig_via_pythagorean (:3122) already solves to the full family (verified). cot(x)+cot(2x)=0 needs cot double-angle handling and is its own cycle.
- **Blast radius:** Low. The Laurent-trig clearing is gated on a negative u-exponent, so pure-polynomial trig equations (already correct via try_solve_polynomial_in_trig) never enter the new path — identical gating discipline to the proven exponential analog. Only previously-failing reciprocal forms change. The sub-shape (b) denominator-clearing must assert the trig denominator is nonzero (sin/cos are never simultaneously zero, and a nonzero-constant RHS keeps roots off the poles), so no extraneous roots. No shared simplifier/core changes required — the fix lives entirely in the solve-algebra layer of solve_backend_local.rs.
- **Maquinaria reutilizable:** try_solve_exponential_reciprocal_polynomial (solve_backend_local.rs:3568) is the near-exact copy template (Laurent map build + exponent shift + clear). solve_polynomial_in_atom (:2165) and union_branch_solutions (:2207) already back-substitute roots through the periodic base solver and combine periodic families over a common period. try_solve_mixed_trig_via_pythagorean (:3122) + rewrite_even_power_of_atom (:3058) solve the cleared numerator for sub-shape (b). find_trig_atom_containing_var (:3996) already locates the substitution atom inside a reciprocal/Div tree. Insertion point: the dispatch at :10726 (right after the exp reciprocal handler, before try_solve_polynomial_in_trig at :10732).
- **ROI:** Biggest solve-algebra cluster (12). Reciprocal/power trig equations reduce correctly to a base equation (sec=2 → cos=1/2) but the reduction returns only principal-value representative(s) via arccos/arcsin, dropping the +period family and/or the companion branch. Two sub-shapes: (a) reciprocal odd/even power collapses to a singleton/finite window (sec^3, csc^3, sec^5, sec^2=1, 1/cos^2=1, 1/sin^2=1); (b) squaring keeps only the +sqrt branch, emitting a lossy garbage residual (cot+cot2x, 1/tan^2). The base solvers cos(x)=c ALREADY emit the full family, so the fix is to route every reciprocal/power reduction through them instead of computing a bare arccos. High reuse; recurring across spellings (prior fixes 9e4e854aa/0a6f52450 do not cover these) so multi-cycle.

### F3 [P1] taylor-removable-singularity-undefined

- **Representante:** `taylor(sin(x)/x, x, 0, 4)`
- **Correcto:** 1 - x^2/6 + x^4/120
- **Miembros (6):** `taylor(sin(x)/x, x, 0, 4)`; `taylor(x/(exp(x)-1), x, 0, 4)`; `taylor((exp(x)-1)/x, x, 0, 4)`; `taylor((1-cos(x))/x^2, x, 0, 4)`; `taylor(tan(x)/x, x, 0, 4)`; `taylor(sinh(x)/x, x, 0, 4)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_math/src/limits_support.rs:4603
- **Mecanismo:** In the Expr::Div branch of taylor_at_zero_with_rational (limits_support.rs:4600-4609), the call reciprocal_series(&den_series, order, var_name)? unconditionally requires den(0) != 0. For a removable singularity like sin(x)/x, den_series = x has coeffs [0,1] (zero constant term), so reciprocal_series returns None and the Div branch declines. taylor_series_at_zero_expr then returns None, and TaylorRule (cas_engine/src/rules/calculus/taylor.rs:49-58) falls through to the differentiation-from-definition fallback taylor_series_at_point_expr. That path evaluates f^(0)(0) = substitute(sin(x)/x, x->0) = sin(0)/0 = 0/0, which fold_constant_subexprs collapses to the 'undefined' sentinel and leaks as the whole result. All 6 members are Div forms where numerator and denominator both vanish at 0 with equal valuation, so all hit this same decline-then-leak path.
- **Boceto de fix:** In the Expr::Div branch, keep the existing path as an early-return when reciprocal_series succeeds (identical behavior for non-singular quotients, vd=0). When it fails (den(0)=0), add a removable-singularity fallback: (1) compute denominator valuation vd = den_series.coeffs.iter().position(|c| !c.is_zero()) — None means den is identically zero, decline; (2) re-expand num and den to order+vd (needed so the quotient still has order+1 coefficients after cancelling x^vd — e.g. sin needs order 5 to recover x^4/120); (3) cancel the common x^vd via a shift_down helper that drops vd leading coeffs from each, returning None if any of the numerator's first vd coeffs is non-zero (numerator valuation < vd = genuine pole, honestly decline); (4) the shifted denominator now has a non-zero constant term, so reciprocal_series succeeds — multiply by the shifted numerator series and truncate_polynomial to order. Two small local helpers (valuation, shift_down); reuses reciprocal_series, truncate_polynomial, Polynomial::{new,mul,coeffs,is_zero}. Entirely within the one Div match arm.
- **Blast radius:** Low. taylor_at_zero_with_rational is a private fn reached only via the public taylor_series_at_zero_expr, which is called only by TaylorRule (the taylor()/series() command at expansion point 0). The limit engine's series path uses the SEPARATE taylor_at_zero (not the _with_rational variant, by design comment at limits_support.rs:4585-4589) and is untouched. The non-singular quotient path is preserved byte-for-byte via early-return. Genuine poles keep declining because numerator valuation < denominator valuation makes shift_down fail: verified 1/x, 1/x^2, (1+x)/x, cos(x)/x, 1/(exp(x)-1), (sin(x)-x)/x^5 all currently return 'undefined' and stay declined. Existing tests rational_taylor_declines_pole_at_zero (10830) and rational_taylor_matches_known_geometric_series (10820) remain green.
- **Maquinaria reutilizable:** reciprocal_series (limits_support.rs:4569, already implements the 1/den power-series recurrence and requires den(0)!=0 — satisfied after cancellation), truncate_polynomial (4817), Polynomial::new/mul/coeffs/is_zero (polynomial.rs). Polynomial::new already trims trailing zeros so valuation (lowest non-zero index) is well-defined. No new algorithms needed.
- **ROI:** The Taylor expander evaluates the operand at the expansion center, hits 0/0, and propagates the operand's NonZero domain condition into a leaked 'undefined' sentinel instead of expanding through the removable singularity. One self-contained series-engine fix — expand numerator and denominator series and divide (or take the limit) when the center is a removable singularity — closes all 6. No solver coupling; clean bounded capability. High ROI.

### F4 [P0] transcendental-inequality-periodicity-drop

- **Representante:** `solve(sec(x)^2>2, x)`
- **Correcto:** ⋃_{k∈ℤ} ((π/4+kπ, π/2+kπ) ∪ (π/2+kπ, 3π/4+kπ))  (poles π/2+kπ excluded)
- **Miembros (6):** `solve(csc(x)^2>2, x)`; `solve(csc(x)^2<2, x)`; `solve(sec(x)^2>2, x)`; `solve(sec(x)^2<2, x)`; `solve(tanh(x)^2<1, x)`; `solve(abs(csc(x))<2, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:7219 (try_solve_even_power_or_abs_trig_inequality — the natural chokepoint to extend); soundness-gate defect at crates/cas_solver/src/solve_backend_local.rs:4084 (contains_trig_of_var)
- **Mecanismo:** see mechanism field
- **Boceto de fix:** Mirror the equation fix 9e4e854aa on the inequality path. Extend try_solve_even_power_or_abs_trig_inequality (7219): (a) make `detect` ALSO match the reciprocal-square shape `Div(A, Pow(sin|cos(g),2))` (reuse the exact `recip_square` matcher already written at 8037), setting an is_reciprocal flag, and invert `A/trig²{op}c ⟺ trig²{op'}A/c` with the 1/x sign-flip logic already present in the power-1 handler (7460-7499); (b) REPLACE the surd `|trig|<√t` reduction with the DOUBLE-ANGLE reduction from the equation reducer at 8104: `sin(g)²{op}t ⟺ cos(2g){flip op}(1-2t)`, `cos(g)²{op}t ⟺ cos(2g){same op}(2t-1)`, then sub-solve `cos(2g){op}rational` through the pipeline — verified this already yields the correct periodic union (e.g. cos(2x)<0 → (π/4+kπ,3π/4+kπ), even symbolic-arccos thresholds work). This alone closes the two `<` members (sec²<2, csc²<2) fully and, as a bonus, bare `sin²/cos²{op}c`. (c) For the reciprocal `>` direction whose answer straddles the pole (trig(g)=0 at the window midpoint), PUNCTURE it: split each PeriodicIntervalUnion window at its midpoint into two open half-windows — new machinery to add to periodic_interval_union.rs (no split/point-subtract helper exists today). BOUNDED SOUNDNESS FLOOR (single cycle, if only stopping wrong answers): add Csc/Sec/Cot (and a hyperbolic-square) arm to contains_trig_of_var:4084 (or the decline handler), converting the 6 wrong answers to honest residuals matching the already-honest 1/sin,1/cos forms.
- **Blast radius:** Extending try_solve_even_power_or_abs_trig_inequality to the double-angle route changes bare `sin²/cos²{op}c` from honest-decline to solved (net improvement, but touches an existing decline behavior — sweep bench fixtures). The reciprocal-square Div match is a new shape with no overlap with the power-1 handler (exp −1 vs 2) or the rational-inequality path (non-polynomial denominator), so it steals nothing. Double-angle reduction only fires for sin/cos-based reciprocals and gates through the boundary solver (which declines/uses symbolic arccos cleanly), so a non-representable threshold falls back to decline — soundness preserved. The pole-puncture window-split in periodic_interval_union.rs is the only riskier change: must keep half-windows OPEN and not disturb the existing intersect/union/package paths; add contract tests for both punctured (`sec²>2`, `csc²>2`) and unpunctured (`sec²<2`, `csc²<2`) members plus the bare-square controls. The contains_trig_of_var extension (soundness floor) has near-zero blast radius (only widens which forms decline).
- **Maquinaria reutilizable:** see reusable_machinery field
- **ROI:** Reciprocal-square trig inequalities are reduced to a boundary equation plus a single monotone ray (or collapse trivially), ignoring periodicity, range, and pole exclusion — producing grossly wrong sets: ∅ (sec^2>2), ℝ (sec^2<2), one bounded ray (csc^2>2), or ∅ where the answer is all of ℝ (tanh^2<1). The reciprocal-square trig EQUATION analog is already fixed (9e4e854aa); this reuses the same sin^2/cos^2 reduction but needs periodic interval-union assembly plus pole puncture on the inequality path. 6 P0 members in common textbook forms.

### F5 [P0] nested-abs-region-enumeration-drop

- **Representante:** `solve(abs(abs(x)-abs(x-2))=1, x)`
- **Correcto:** {1/2, 3/2}
- **Miembros (8):** `solve(abs(abs(x)-abs(x-2))=1, x)`; `solve(abs(abs(x)-abs(x-2))=2, x)`; `solve(abs(2*abs(x)-abs(x-1))=1, x)`; `solve(abs(abs(2*x)-abs(x-3))=1, x)`; `solve(abs(abs(x)-2)/abs(x)=1, x)`; `solve(abs(abs(x)-3)/abs(x)=1, x)`; `solve(abs(abs(x-2)-abs(x+2))=2, x)`; `solve(abs(abs(x-1)-abs(x-3))=1, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:12081 (Discrete-only bail in try_solve_abs_equality, fn at :12039) + its invocation gate at :11007
- **Mecanismo:** `abs(E) = c` with E = a difference/sum of >=2 abs-of-linear terms is caught by no specialized handler in solve_local_core_inner, so it falls to the generic function-isolation, which splits |E|=c into E=c / E=-c and re-solves each branch via the NARROW per-branch recursion. That recursion isolates a single inner abs (`|x| = |x-2| ± c`) instead of the exact sum-of-abs segment solver; when the isolated variable RHS `|x-2| ± c` can go negative it returns Empty (or a spurious guarded ray), so one branch's roots/ray are dropped. Which branch drops varies (member1 keeps {3/2}, member3 keeps {0}), proving per-branch lossiness. Smoking gun: the =2 case leaks the isolate-one-abs guard `if |x-2|+2>=0`, not the clean [2,inf) the segment solver produces standalone. A recovery exists (try_solve_abs_equality, :12039) that re-splits |arg|=±c and re-solves each branch through the FULL solver (solve_relation_set -> solver_entrypoints_solve::solve, which reaches try_solve_sum_of_abs_relation/solve_decomposed_abs_relation and solves BOTH branches correctly). But it is doubly broken: (1) its invocation gate at :11007 only fires when isolation returned Residual/Conditional, so member1's clean-but-incomplete Discrete {3/2} never triggers it; (2) its Discrete-only collection at :12075-12102 does `_ => return None` on any non-Discrete branch, so the ray cases (=2: both branches Continuous) bail and the wrong Conditional survives.
- **Boceto de fix:** Bounded fix closes 6/8 (members 1,2,3,4,7,8), all `abs(E)=const`. Two small edits to the existing recovery, no new math (the full-solve per-branch already reaches the exact BigRational segment core solve_decomposed_abs_relation): (a) In try_solve_abs_equality (solve_backend_local.rs:12039), replace the Discrete-only collection loop (12075-12102) with union_solution_sets(ctx, pos_branch, neg_branch) over the FULL branch SolutionSets, keeping the c<0->Empty and c=0->single-branch shortcuts; this makes ray/interval branches (=2) union correctly. (b) Make the recovery fire for nested-abs even when isolation returned a clean Discrete: either broaden the gate at :11007, or (cleaner) add try_solve_abs_equality as a PROACTIVE handler just after try_solve_sum_of_abs_relation (:10651), gated to eq.lhs == abs(E) where E contains a nested abs -- e.g. decompose_sum_of_abs(E) yields >=2 abs terms -- so the working abs(linear)=c and single-inner-abs ||x|-3|=1 paths stay untouched. Members 5,6 (abs(|x|-c)/abs(x)=1) are a SEPARATE second cycle: the cleared equation abs(abs(x)-2)=abs(x) already returns {-1,1}, so the fix there is normalizing abs(N)/abs(D)=k to abs(N)=k*abs(D) before solving -- unrelated to nested-abs enumeration.
- **Blast radius:** Low. Gated to `abs(<nested multi-abs>) = const`. The full-set union is a strict generalization of the current Discrete-only union (the existing |x^2-2x|=3 -> {-1,3} recovery still unions two Discrete sets to the same result). The nested-abs gate (>=2 inner abs terms) keeps the already-correct abs(linear)=c and single-inner-abs (||x|-3|=1 -> {4,-4,2,-2}) forms on their untouched isolation paths. No new recursion path (solve_relation_set is the existing full-solve entry; E=c has no bare outer abs so it cannot re-enter try_solve_abs_equality -> no infinite recursion). Verify huella on abs-equation fixtures.
- **Maquinaria reutilizable:** try_solve_abs_equality (split-and-resolve structure, solve_backend_local.rs:12039); solve_relation_set (:1062) -> solver_entrypoints_solve::solve full dispatch -> try_solve_sum_of_abs_relation (solve_outcome.rs:415) / solve_decomposed_abs_relation (:451), the exact BigRational per-segment breakpoint solver that already yields both the point roots and the flat-region rays; union_solution_sets (cas_solver_core::solution_set); extract_scaled_abs (solve_outcome.rs:160) and decompose_sum_of_abs (:281) for the nested-abs detection gate.
- **ROI:** Nested / multiple absolute-value equations: the piecewise sign-split enumeration covers some breakpoint regions but silently drops others — typically the symmetric-partner root (returns {3/2} not {1/2,3/2}, {-1} not {-1,1}), or the constant-|f|=c interval region (the =2 case drops a whole ray). The engine solves the FLATTENED equivalent correctly, so the defect is localized to nested-abs region enumeration. One fix — enumerate every sign combination of all inner abs breakpoints, solve and domain-filter per region — closes all 8. 8 P0 members, common form, high reuse.

### F6 [P0] modular-exponentiation-missing

- **Representante:** `mod(123456789^987654321, 1000000007)`
- **Correcto:** 652541198
- **Miembros (3):** `mod(6^50000000, 100)`; `mod(123456789^987654321, 1000000007)`; `mod(10^100000000, 7)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_engine/src/engine/transform/transform_helpers.rs:3855
- **Mecanismo:** The `eval` simplifier (`LocalSimplificationTransformer`) is strictly bottom-up: `transform_expr_recursive` at crates/cas_engine/src/engine/transform/mod.rs:277 says "1. Simplify children first (Bottom-Up)". So for `mod(a^b, m)` it recurses into and fully evaluates the child `Expr::Pow(a,b)` BEFORE the `mod` Function node's rules ever run. A simplifier rule factors the integer base into prime powers (`6^n → 2^n·3^n`, verified: `eval "6^1001"` → `2^1001·3^1001` with the power-of-2 factor materialized as an exact BigInt, odd factors left symbolic) and materializes `2^n` uncapped via bit-shift. For the family's exponents (5e7, 9.8e8, 1e8) that BigInt has tens of millions of digits, so construction/printing hangs for minutes (verified: representative and all 3 members hang past a 25 s timeout; Python `pow(...,...,m)` gives the family's stated answer 652541198). Note the `const_eval` literal-power path DOES cap (MAX_ABS_POW=1000, crates/cas_math/src/const_eval.rs:19/37) and declines, so const-folding is not the leak — the transformer's base-prime-power rule is. The `mod` handler itself (`try_eval_simple_number_theory_call` line 107 → `compute_mod_expr` at crates/cas_math/src/number_theory_support.rs:1242) is architecturally incapable of preventing this: it only reads an ALREADY-materialized `Number` via `extract_integer_bigint`/`extract_integer_exact` (crates/cas_math/src/expr_extract.rs:63, which accepts only `Expr::Number`/`Neg`, never expands a `Pow`). By the time that handler runs, the hang has already happened during child simplification. A fix confined to the mod handler is therefore too late — the interception must happen BEFORE the Pow child is recursed into.
- **Boceto de fix:** Add a raw-tree pre-child interception in `transform_function` (crates/cas_engine/src/engine/transform/transform_helpers.rs), alongside the existing early-return guards that deliberately do NOT simplify a child (`is_hold_all_function` line 3821, `diff_call_should_preserve_raw_target_for_direct_derivative` line 3829, `integrate_call_should_preserve_raw_target_for_direct_integration` line 3839) — i.e. insert BEFORE the "Simplify children" loop at line 3855. When `name == "mod"`, `args.len() == 2`, `args[0]` is `Expr::Pow(base, exp)` with `base` and `exp` extractable as integer literals (via existing `extract_integer_exact`), `exp >= 0`, and `args[1]` an integer literal `m != 0`: compute `r = base.modpow(&exp, &m.abs())` and Euclidean-normalize to `[0,|m|)` exactly as `compute_mod_expr` already does (`((r % m) + m) % m`), then `return self.context.add(Expr::Number(BigRational::from_integer(r)))`. Any input not matching this shape falls through unchanged to normal bottom-up simplification. Keep it minimal: decline (fall through) on negative exponent (would need a modular inverse — `compute_modinv_expr` at number_theory_support.rs:917 exists if a follow-up wants it) and on non-integer/symbolic operands. Best implemented as a small helper `fn try_modpow_shortcut(ctx, name, &args) -> Option<ExprId>` in cas_math (next to `compute_mod_expr`) so both the eval transformer and, if desired, the solver `derive` path (crates/cas_solver/src/derive_command.rs, which also dispatches number-theory calls) can call it.
- **Blast radius:** Very narrow. The guard fires only for the exact shape `mod(<integer>^<nonneg-integer>, <nonzero-integer>)`; every other `mod(...)` (including `mod(a+b,m)`, symbolic args, small already-reducible powers) takes the unchanged bottom-up path. The result it produces is numerically identical to the current materialize-then-reduce answer for cases small enough to complete today, so no correct output changes — it only replaces a hang/unreduced-residual with the correct value. No new rule ordering, no phase interaction, no domain/branch subtleties (pure integer arithmetic). Reuses `num_bigint 0.4.6`'s `BigInt::modpow` and the existing Euclidean-normalization convention. Regression risk essentially nil; main care items are (a) mirroring the existing sign/normalization convention of `compute_mod_expr` and (b) declining cleanly on negative exponents rather than panicking (BigInt::modpow panics if no inverse exists).
- **Maquinaria reutilizable:** num_bigint 0.4.6 `BigInt::modpow` (binary modular exponentiation, no dependency added); existing u64 fast modexp `crate::modp::n(base,exp,p)` at crates/cas_math/src/modp.rs as a reference/fast-path; `extract_integer_exact` (crates/cas_math/src/expr_extract.rs:63) and `extract_integer_bigint` (number_theory_support.rs:621) for operand extraction; the Euclidean normalization `((a % n) + n) % n` already in `compute_mod_expr` (number_theory_support.rs:1248); `compute_modinv_expr` (number_theory_support.rs:917) available if a later cycle extends to negative exponents; the pre-child raw-target-preservation pattern already established at transform_helpers.rs:3821-3853 (diff/integrate/hold-all) is the exact template to copy.
- **ROI:** mod(a^b, m) fully materializes a^b instead of using fast modular exponentiation, so large exponents hang for minutes (and above the ~1000-exponent cap the materialized power is itself f64-contaminated garbage). One bounded, classic fix — pattern-match mod(pow(a,b), m) and evaluate by binary modular exponentiation — closes all 3 hangs on standard competition/number-theory inputs. Cheap, decoupled, P0 hang: strong ROI.

### F7 [P0] abs-inequality-symbolic-center

- **Representante:** `solve(abs(x-a) <= 3, x)`
- **Correcto:** [a-3, a+3]
- **Miembros (3):** `solve(abs(x-a) < 3, x)`; `solve(abs(x-a) <= 3, x)`; `solve(abs(x-a) > 3, x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:6090 (try_solve_abs_threshold_inequality — the numeric-threshold abs-inequality reduction); root-cause chokepoint crates/cas_solver_core/src/solution_set.rs:105 (compare_values structural fallback)
- **Mecanismo:** `|k·x + b| {op} c` with a SYMBOLIC center (b symbolic, c a numeric constant) is never assembled directly; it reaches the interval set-algebra whose endpoint ordering is value-blind for symbolic endpoints.

Routing: (1) try_solve_abs_vs_symbolic_param (solve_backend_local.rs:9862), the DIRECT-construction handler that fixed the symbolic-THRESHOLD twin ("built DIRECTLY, never through the set algebra"), DECLINES here — line 9976 returns None because the threshold c=3 is a rational constant, and its affine extraction Polynomial::from_expr (line 10014) requires rational coeffs so it fails on the symbolic constant term of x−a. (2) try_solve_abs_threshold_inequality (6090) reduces |g| op c via solve_concrete_side + intersect_solution_sets/union_solution_sets, i.e. it hands the symbolic-endpoint rays to the set algebra. (3) Whatever isn't caught falls to solve_inner (line 10886) generic abs isolation, which also combines through the set algebra. The pre-fix degenerate signature is even documented at solve_backend_local.rs:9852-9854 (< → No solution, ≤ → [e,e]∪[e,e], > → AllReals).

Root defect: intersect_intervals/merge_intervals/point_in_interval (solution_set.rs) order endpoints via compare_values (solution_set.rs:39). For endpoints carrying the free parameter a, no value oracle (surd/nth-root/const-bounds) applies, so it falls to the value-blind structural cas_ast::ordering::compare_expr (line 105). compare_expr ranks by node kind FIRST (ordering.rs:124-140: Add=8, Sub=9): a−3 is a Sub node (rank 9), a+3 is an Add node (rank 8), so compare_values(a−3, a+3)=Greater — the band [a−3,a+3] is judged reversed/empty. Numeric center works because numeric endpoints order by value. |x+a| happens to work only because its lower endpoint −a−3 gets a lower structural rank than 3−a (luck), not because the algebra is correct. Proven independently of abs: solve((x-(a-3))*(x-(a+3)) <= 0) → [a+3, a-3] (reversed empty interval) and > 0 → overlapping rays ≈ ℝ. Note simplify((a+3)-(a-3))=6, so the order is decidable by difference-sign, but compare_values only has &Context (no Simplifier) and cannot fold the free a out of (a−3)−(a+3), which is why the core deliberately cannot order symbolic endpoints and the established fix builds bands directly at the solver layer.
- **Boceto de fix:** Build the symbolic-center band DIRECTLY at the solver layer (where a Simplifier exists), bypassing the untrustworthy set algebra — exactly the pattern already used by try_solve_abs_vs_symbolic_param for the symbolic-threshold twin. Preferred site: teach try_solve_abs_threshold_inequality (6090) — the reduction the ROI names — to, when g is affine k·x+b (k rational≠0, b var-free, possibly symbolic) and c is numeric, compute the two boundary endpoints e_c=(c−b)/k and e_negc=(−c−b)/k, ORDER them by the sign of the rational slope k (decidable; never a symbolic compare), and assemble the result directly: c>0 → </≤ = Continuous(open/closed (lo,hi)), >/≥ = Union[(-∞,lo),(hi,∞)]; c=0 and c<0 handled by the existing sign shortcuts already in that function (lines 6132-6154). This reuses the invert+orient+assemble block already written in try_solve_abs_vs_symbolic_param (lines 10029-10090): the only new piece is peeling an affine k·x+b with a SYMBOLIC constant term b (Polynomial::from_expr is rational-only; use the additive-constant peel at 9897-9945 + peel_rational_coefficient at 6599 to split the rational x-coefficient from the var-free remainder). Because the endpoints are placed by the rational slope's sign, compare_values is never consulted, so the family closes regardless of the compare_expr rank bug. Alternative equivalent site: extend try_solve_abs_vs_symbolic_param to also fire for numeric c≥0 with a symbolic-constant affine argument (drop the rational-c decline at 9976 for this shape, decide c's sign by value → no guard). Gate tightly to a symbolic (non-rational) constant term so numeric-center forms keep their current owner and pinned fixtures are untouched.
- **Blast radius:** LOW. A new/extended handler gated to |affine(x)| {op} numeric-c whose argument has a SYMBOLIC (non-rational) constant term. Numeric-center forms (as_rational_const of the whole argument succeeds) keep their existing owner, so every pinned numeric abs-inequality fixture is untouched (verified: abs(x-2) forms still correct). Must place it BEFORE solve_inner and ensure it does not preempt the numeric-center path. Risk items: (1) rational-slope variants must be covered too — abs(2*x - a) > 3 is currently also wrong (→ ℝ), so orient by k's sign, not just k=1; (2) c=0 and c<0 edge cases (reuse the existing sign shortcuts). Regression surface is limited to symbolic-parameter abs inequalities, a small family. A deeper alternative — making compare_values order symbolic endpoints by decidable difference-sign — would additionally close the sibling symbolic-endpoint families (product/quadratic inequalities like (x-(a-3))*(x-(a+3))≤0), but it is blocked by the &Context-only core lacking symbolic simplification to fold (a−3)−(a+3)→−6, so it would require solver-layer comparator plumbing and carries much broader regression risk: that path is multi-cycle and should NOT be attempted as the bounded fix.
- **Maquinaria reutilizable:** try_solve_abs_vs_symbolic_param invert/orient/assemble block (solve_backend_local.rs:10029-10090) — direct interval/union construction for affine abs relations; additive-constant peeling (9897-9945) and peel_rational_coefficient (6599) for extracting k·x+b with a symbolic remainder; match_abs_argument (5205); flip_inequality; Interval::open/closed and SolutionSet::{Continuous,Union}. The parametric sign oracles (root_forms::provable_sign_vs_zero, const_sign::provable_const_sign) are available if a mixed symbolic/proven-sign threshold ever needs deciding, but are NOT needed for a numeric c.
- **ROI:** |x−a| ⋛ c with a SYMBOLIC center: the abs-inequality reduction only handles a numeric center and falls through to solving the boundary equation |x−a|=c, mis-assembling the result into ∅ (for <), the two bare endpoints {a−3,a+3} (for ≤), or ℝ (for >). No ordering ambiguity exists (the answer is always the band (a−c,a+c) or its complement), so the fix is simply to let the existing reduction carry a symbolic center. 3 P0 members, bounded, high-confidence.

### F8 [P0] leibniz-diff-under-integral

- **Representante:** `diff(integrate(x*sin(t^2), t, 0, 1), x)`
- **Correcto:** integrate(sin(t^2), t, 0, 1)  (≈ 0.310268, a nonzero constant — NOT 0)
- **Miembros (6):** `diff(integrate(x*sin(t^2), t, 0, 1), x)`; `diff(integrate(1/(t+x), t, 0, 1), x)`; `diff(integrate(sin(x*t)/t, t, 1, x), x)`; `diff(integrate(exp(x*t^2), t, 0, x), x)`; `diff(integrate(exp(-t^2)*cos(x*t), t, 0, oo), x)`; `diff(integrate(1/(t^2+x), t, 0, oo), x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_engine/src/rules/calculus/integral_derivative_shortcut_presentation.rs:389 (in fn definite_integral_leibniz_diff_rewrite, spanning lines 348-399; secondary undefined-leak site at lines 372-373)
- **Mecanismo:** `definite_integral_leibniz_diff_rewrite` implements only the BOUNDARY half of the Leibniz rule. It builds `result = f(b(x))*b'(x) - f(a(x))*a'(x)` (lines 384-389) and never adds the parameter-differentiation term `+ ∫_{a(x)}^{b(x)} ∂f/∂x(t,x) dt`. This function is the sole handler for `diff(integrate(f,t,a,b), x)`: `try_extract_integrate_call` only matches 1/2-arg indefinite integrals (symbolic_calculus_call_support.rs:44-72), so the earlier `supported_integral_diff_shortcut_rewrite` and reciprocal-trig shortcut both bail on 4-arg definite integrals, and the definite integral itself is held (the definite integrator cannot evaluate any of the 6 members, even the elementary-in-t `1/(t+x)`). The single omission produces all three observed symptoms: (1) constant bounds + x-dependent integrand => boundary terms are 0 => returns a concrete WRONG 0 (`x*sin(t^2)` on [0,1], `1/(t+x)` on [0,1]); (2) x-dependent bounds + x-dependent integrand => returns only the boundary term, dropping the integral (`sin(x*t)/t` on [1,x] gives sin(x^2)/x but drops ∫cos(xt); `exp(x*t^2)` on [0,x] gives e^(x^3) but drops ∫t^2 e^(x t^2)); (3) infinite bound => `diff(ctx, upper)` where upper=oo returns Some(undefined) (not None, so the `?` doesn't short-circuit) and `f_at_upper * undefined` leaks `undefined` for the [0,oo] members. Cases where the definite integral IS elementary in t (e.g. `integrate(cos(x*t),t,0,1)`=sin(x)/x) are pre-evaluated before diff runs and are correct — the bug only surfaces on the non-elementary/parameter integrands this rule was written to catch; it was left half-finished.
- **Boceto de fix:** Complete the Leibniz rule in this one function (~15-25 lines): (a) compute the partial `df_dx = differentiate_symbolic_expr(ctx, integrand, &diff_var)` — this ALREADY does partial differentiation treating integration var t as independent (verified: diff(x*sin(t^2),x)=sin(t^2)); if `df_dx` is non-zero, build a fresh held `integrate(df_dx, int_var_expr, lower, upper)` node (4-arg Function) and add it to the result (result = Sub(boundary) + param_integral). When df_dx folds to 0 (integrand independent of x) the term vanishes and behavior is byte-identical to today, preserving the passing sin(t^2)/e^(t^2) cases. (b) Fix the boundary construction so a bound NOT containing diff_var contributes 0 without calling diff on it — only compute f(b)*b' when upper mentions diff_var, else skip; same for lower. This kills the undefined leak from diff(oo) (a constant bound genuinely doesn't move, so its boundary contribution is exactly 0). Net per member: case1=>integrate(sin(t^2),t,0,1) (exact stated-correct honest residual); case2=>integrate(-1/(t+x)^2,t,0,1); case3=>sin(x^2)/x + integrate(cos(x*t),t,1,x) (inner evaluates to closed form); case4=>e^(x^3) + integrate(t^2*e^(x*t^2),t,0,x); cases5/6=>guarded boundary 0 + held integrate(∂f/∂x,t,0,oo). Non-elementary inner integrals stay held as honest residuals (a nonzero held constant, not 0) — exactly the correct family answer.
- **Blast radius:** Low. The change adds a term ONLY when ∂f/∂x != 0 (integrand depends on the diff var); existing correct x-independent cases (diff(integrate(sin(t^2),t,0,x),x)=sin(x^2), and the e^(t^2) variant) get df_dx=0, term vanishes, output unchanged — the two existing unit tests (leibniz_rule_differentiates_non_elementary_definite_integral, leibniz_rule_refuses_when_integration_var_is_diff_var) still pass. The constant-bound guard only changes the oo-leak (constant finite bounds already folded f(a)*0 to 0). No recursion risk: the emitted integrate(∂f/∂x,...) is a plain definite-integral node, not wrapped in diff, so it cannot re-enter definite_integral_leibniz_diff_rewrite; the engine already holds such nodes (no loop). Watch item: confirm no other test pins the current partial/0 answers (none found in this module) and that emitting a held 4-arg integrate through finalize_diff_rewrite_with_conditions doesn't trip the definite-integrator convergence handling for the oo cases.
- **Maquinaria reutilizable:** All present, no new capability needed: (1) cas_math::symbolic_differentiation_support::differentiate_symbolic_expr already computes the partial ∂f/∂x treating t as independent (already invoked in this same function for bound derivatives). (2) DefiniteIntegralCall extractor already yields integrand/var_expr/lower/upper. (3) ctx.add(Expr::Function(integrate_sym,[df_dx,var_expr,lower,upper])) builds the residual term; the normal integration pipeline then evaluates it when elementary or holds it honestly when not (verified: integrate(cos(x*t),t,1,x) => closed form; integrate(sin(t^2),t,0,1) => held). (4) finalize_diff_rewrite_with_conditions already wires the result back. The boundary-term scaffold (substitute_power_aware, bound differentiation, Sub construction) is already written — only the parameter term and constant-bound guard are missing. NOTE: the family JSON labels this multi-cycle, but the boundary-Leibniz scaffolding already exists in-tree, so remaining work is genuinely bounded-single-cycle; I flag the JSON estimate as an overstatement.
- **ROI:** diff w.r.t. x of a HELD (unevaluated) definite integral whose integrand and/or limit depends on x. The engine treats the held integral node as constant in x → returns 0 (concrete wrong value), or applies FTC to the limit but drops the ∂/∂x-integrand term (partial Leibniz), or leaks 'undefined'. One capability — implement the Leibniz rule on the held definite-integral node (boundary terms + ∫ ∂/∂x integrand dt) — closes all 6, with an honest residual when the resulting integral is non-elementary. Several are outright wrong 0s (P0). Costs a real calculus capability, hence multi-cycle.

### F9 [P0] product-inequality-symbolic-root-ordering

- **Representante:** `solve((x-1)*(x-a) > 0, x)`
- **Correcto:** (-∞, min(1,a)) ∪ (max(1,a), ∞); for a=1 it is all reals except x=1
- **Miembros (5):** `solve((x+a)*(x-a) > 0, x)`; `solve((x-1)*(x-a) > 0, x)`; `solve((x-2)*(x-a) < 0, x)`; `solve((x-a)*(x-b)*(x-c) > 0, x)`; `solve((x-a)^2*(x-b) > 0, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/solution_set.rs:379 (finalize_product_zero_inequality_solution_set — the assembler chokepoint); mechanism origin at solution_set.rs:105 (compare_values value-blind structural fallback)
- **Mecanismo:** A factored inequality like (x-1)*(x-a)>0 is already a Mul, so it takes the product-zero split path (isolation_utils::should_split_product_zero_inequality -> solve_outcome::plan_product_zero_inequality_split), producing case1 = (x-1>0 ∧ x-a>0) and case2 = (x-1<0 ∧ x-a<0). These branch SolutionSets are combined by finalize_product_zero_inequality_solution_set (solution_set.rs:379), which calls intersect_solution_sets -> intersect_intervals (solution_set.rs:549). intersect_intervals computes max/min of the interval endpoints via compare_values(1, a) (solution_set.rs:553/567/581). For a symbolic endpoint a, every value oracle in compare_values misses (not a Number, not a quadratic/nth-root surd, no const_value_bounds), so it falls to the TOTAL structural fallback cas_ast::ordering::compare_expr at solution_set.rs:105. That returns a tree-structure order (here it silently decides 1<a) and the assembler commits UNCONDITIONALLY, yielding (-inf,1) U (a,inf). That set is valid only in the regime a>1; for a<1 the truth is (-inf,a) U (1,inf) and for a=1 it is R\{1}, so the other regimes get a grossly wrong set with empty required_conditions. Same root cause for the ternary (x-a)(x-b)(x-c)>0 -> (a,b)U(c,inf) (assumes a<b<c) and for the double-root (x-a)^2(x-b)>0 -> (b,inf), whose puncture at a is dropped because a in (b,inf) hinges on the same undecidable compare_values(a,b). Numeric controls ((x-1)(x-3), reversed order, <) are all correct because compare_values decides them via the Number branch — only value-incomparable symbolic endpoints break.
- **Boceto de fix:** Guard the product-zero inequality assembler against committing to a guessed symbolic-endpoint ordering (see mechanism field for full detail); reuse try_compare_values to detect the incomparable case and emit an honest Residual echo at the pipeline site that owns &mut Context and the original operands.
- **Blast radius:** finalize_product_zero_inequality_solution_set is used ONLY by the product-zero inequality plumbing (grep confirms: solve_outcome.rs, isolation_arithmetic.rs, its def in solution_set.rs) — a guard there affects exactly this family. Because the guard fires only when try_compare_values returns None (value-undecidable symbolic endpoints), every numeric, surd, and constant-transcendental-endpoint product inequality keeps its current (correct) answer; the only behavior change is that today's wrong symbolic sets become an honest Residual/echo (or a Conditional). Regression risk LOW for the Residual-decline variant since Residual is already a handled SolutionSet shape. The main cost/risk is the Conditional variant, which touches downstream formatter/verification/step-trace handling of inequality-derived Conditionals. Threading the echo expr (or the case-split) through the deeply generic mul-isolation pipeline closures (isolation_arithmetic.rs 519-597 / 799-869) is the fiddly part that pushes this past a trivial one-liner.
- **Maquinaria reutilizable:** try_compare_values (solution_set.rs:118); SolutionSet::Residual and SolutionSet::Conditional with ConditionPredicate::Positive/EqZero (cas_ast/domain.rs); the shared plan/finalize_product_zero_inequality plumbing (solve_outcome.rs, isolation_arithmetic.rs) that all 5 members route through.
- **ROI:** Factored-polynomial inequalities with symbolic roots: the sign-chart/interval assembler commits UNCONDITIONALLY to one root ordering (or drops the abs for a sign-agnostic constant, or drops the double-root puncture), valid only under an undecidable sign(a−b). It yields grossly wrong sets (e.g. AllReals) for the other regime and returns them with empty required_conditions. Fix needs a sign-of-difference case-split (or an honest residual with an attached ordering condition) in the sign-chart assembler — reusable across all these forms. 5 P0 members; case-split infrastructure makes it multi-cycle.

### F10 [P1] partial-function-domain-constraint-dropped

- **Representante:** `solve(sqrt(a-x)=x, x)`
- **Correcto:** { (sqrt(4a+1)-1)/2 } for a≥0; the algebraic root (-sqrt(4a+1)-1)/2 is extraneous (always < 0)
- **Miembros (4):** `solve(sqrt(a-x)=x, x)`; `solve(sqrt(a-x)=x-1, x)`; `solve(sqrt(x)+3=y, x)`; `solve(asin(x)+acos(x)=pi/2, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:378 (the `sign_vs_zero` closure inside `root_violates_required_condition`, lines 378-399)
- **Mecanismo:** Radical equations record the correct range condition `NonNegative(g)` (g = RHS) at solve_backend_local.rs:10929 via collect_radical_split, and filter_real_solutions (l.244) → root_violates_required_condition (l.362) substitutes each candidate root into g and asks the `sign_vs_zero` closure (l.378-399) to prove the sign. That closure ONLY consults constant/rational-radicand oracles — provable_sign_vs_zero (needs rational A+B√n), provable_sign_vs_zero_const_radicand (constant radicand), and const_sign::const_value_bounds (constant). For the symbolic-parameter root `(−√(4a+1)−1)/2` the radicand `4a+1` is symbolic, so all three return None ⇒ the root is KEPT ⇒ the always-negative extraneous root leaks. The concrete versions already pass because their roots are rational (check_root f64 back-sub, l.206) or fold to a named constant like φ (const_value_bounds), which the numeric path CAN decide — that asymmetry is exactly the 'symbolic path drops the filter the numeric path applies' shape. Members 3 (sqrt(x)+3=y → {(y−3)²}, missing y≥3) and 4 (asin+acos=π/2 → AllReals not [−1,1]) are separate mechanisms: #3's condition is purely on the parameter (substituting the root leaves `y−3` unchanged, so it can't be a filter — it must be EMITTED as a Conditional), and #4's |x|≤1 cannot even be represented (ImplicitCondition in domain_condition.rs:17 has no UpperBound/Range variant) and AllReals is never intersected with domain conditions (filter_real_solutions only maps AllReals→Empty on contradiction, l.318).
- **Boceto de fix:** Closes the representative sub-family (members 1 & 2 — the spurious extraneous negative root under a symbolic parameter): (1) In the `sign_vs_zero` closure at solve_backend_local.rs:378-399 add a final fallback that decides the sign structurally: call crate::solver_entrypoints_proof_verify::prove_positive(ctx, at, RealOnly) → Proven ⇒ Ordering::Greater; else build neg = ctx.add(Expr::Neg(at)) and prove_positive(neg, RealOnly) → Proven ⇒ Ordering::Less; else None. This reuses the exact pattern already at l.9982-9998, so it stays a proof (never a float), preserving the 'only drop on a proof' contract. (2) For that fallback to actually fire on `(−√(4a+1)−1)/2`, teach the structural prover that a real principal square root is nonnegative: in cas_math/src/prove_sign.rs, the sqrt branch of prove_nonnegative_depth_inner (l.604-608) should return TriProof::Proven in real_only instead of recursing into the radicand's sign. Because a blanket change there is risky (see blast radius), prefer scoping it to the filter context — e.g. a real_only 'radicals-are-nonnegative' assumption passed only from the root-filter caller, or a dedicated prove-negative helper in cas_solver that treats Pow(_,1/2)/sqrt(_) as ≥0. With (1)+(2), prove_positive((√(4a+1)+1)/2)=Proven ⇒ root proven <0 ⇒ dropped. Members 3 and 4 are explicitly deferred (multi-cycle): #3 needs a Conditional-emission path when the leftover NonNegative condition contains no solve-var; #4 needs a new UpperBound/Range ImplicitCondition variant plus an AllReals∩conditions intersection in filter_real_solutions.
- **Blast radius:** Change (1) (wiring prove_positive into the closure) is low-risk and additive: it only ever DROPS a root on a Proven proof, and only for roots that already carry a recorded domain condition, so it cannot remove a valid root the previous oracles kept (they returned None → kept; a proof is strictly more information). Change (2) is the risky part: making prove_nonnegative treat √(_) as ≥0 UNCONDITIONALLY in the shared cas_math::prove_sign would change a widely-consumed prover (domain oracle, simplifier, inequality solver) and is unsound for callers deciding definedness — e.g. solve(sqrt(x)≥0) must be [0,∞), not AllReals, and √u≥0 must not be used to erase a u≥0 domain guard. Hence (2) must be scoped to the extraneous-root FILTER context (where the candidate is already assumed a real solution, so its radicals are real and ≥0) rather than applied globally, and requires a full cargo test --workspace + scorecard huella run. Members 3 & 4 touch the SolutionSet/Conditional emission and the ImplicitCondition enum respectively — broader surface, separate cycles.
- **Maquinaria reutilizable:** crate::solver_entrypoints_proof_verify::prove_positive(ctx, expr, ValueDomain::RealOnly) → Proof::Proven — the fully-wired structural sign prover (runtime proof simplifier + prove_nonzero), already used verbatim with the Neg-trick at solve_backend_local.rs:9982-9998 to decide symbolic sign both ways. Underlying cas_math/src/prove_sign.rs prove_positive_depth_inner / prove_nonnegative_depth_inner already handle Add/Sub/Mul/Div/Neg/Pow/sqrt recursion. collect_radical_split (l.905) and the NonNegative(g) recording (l.10905-10934) already exist and are correct. For member 4, intersect_inequality_with_function_domain (l.12338) is a partial precedent for folding a function domain into a result set.
- **ROI:** When a partial function's implicit domain/range constraint must be enforced — sqrt(·)≥0 ⇒ RHS≥0, sqrt-arg ⇒ a shifted parameter bound like y≥3, arcsin/arccos domain |x|≤1 — the SYMBOLIC/identity path drops it while the numeric path applies it. Result: an unfiltered extraneous negative root, a missing parameter condition, or an AllReals over-claim (asin+acos=π/2 → ℝ instead of [−1,1]). Fix: thread the same sign/domain filter used in the numeric branch through the symbolic branch. 4 members (spurious-root / missing-constraint); spans radical squaring-filter and inverse-trig domain so multi-cycle.

### F11 [P0] simplifier-expand-factor-oscillation-hang

- **Representante:** `1/(1+2^(1/3)+4^(1/3))`
- **Correcto:** 2^(1/3) - 1  (≈ 0.259921)
- **Miembros (4):** `1/(1+2^(1/3)+4^(1/3))`; `1/(1+3^(1/3)+9^(1/3))`; `1/(a+b*2^(1/3)+c*4^(1/3))`; `diff((sin(x)/x)^2, x, 2)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_math/src/rationalize_diff_squares_support.rs:412 (try_rewrite_generalized_rationalization_expr / GeneralizedRationalizationRule) — primary fix site for the 3 surd members. Contributing chokepoints for the shared oscillation/halt-failure: crates/cas_engine/src/engine/transform/rule_application.rs:167 (per-step relative anti-worsen gate) and :354 (recurse-on-rewrite deepening); crates/cas_engine/src/engine/transform/mod.rs:211 (depth-50 guard truncates but does not halt); crates/cas_engine/src/orchestrator.rs:26256 (inter-iteration hash guard defeated by monotonic growth).
- **Mecanismo:** GeneralizedRationalizationRule (phase CORE|POST, so it fires on plain eval) rationalizes a >=3-term denominator by the difference-of-squares grouping conjugate = (t1+..+t_{k-1}) - t_k, new_den = group^2 - last^2. This converges ONLY for square-root terms: squaring a sqrt removes one radical, so repeated application is a finite degree-2 field tower (verified: 1/(1+sqrt2+sqrt3) -> 1/4*(sqrt2+2-sqrt6), 0.01s). For cube/nth-roots (n>=3) squaring maps the cubic field to itself (2^(1/3)->2^(2/3), 4^(1/3)->4^(2/3)=2*2^(1/3)), so group^2-last^2 stays irrational and >=3-term; the rule re-fires every pass, each time multiplying the numerator by yet another conjugate factor (the depth-overflow expr shows 3 stacked conjugate factors and a huge expanded surd denominator). This unbounded growth is the fuel for the C5 expand<->factor oscillation (Extract Common Multiplicative Factor / Factor Common Integer vs Combine Like Terms / N-ary Mul Combine Powers). It is not halted because: (1) the anti-worsen budget at rule_application.rs:167 is a per-rewrite RELATIVE gate (>30 nodes AND >1.5x) that a <1.5x-per-step ratchet slips past cumulatively; (2) each successful rewrite recurses via transform_expr_recursive on the whole rewritten tree (rule_application.rs:354), climbing current_depth until the depth-50 guard (mod.rs:211) truncates the innermost descent — but the surrounding growth continues, so it logs 'depth_overflow ... unsimplified' once and keeps spinning; (3) the run_phase inter-iteration semantic-hash cycle guard (orchestrator.rs:26256) never trips because the expression grows monotonically (fresh hash each pass); (4) the cooperative time deadline does NOT reach this hot path — --time-budget-ms 500 still runs 20s. The diff((sin(x)/x)^2,x,2) member is a DIFFERENT mechanism: no rationalization at all (trace: 2215 N-ary Mul Combine Powers + 282 Combine Like Terms + 57 Double Angle Contraction) — the pure auto-expand difference-quotient oscillation, an orchestration-level problem.
- **Boceto de fix:** Bounded win closing the 3 surd members (low risk): gate try_rewrite_generalized_rationalization_expr (and, defensively, the diff-squares sibling) to DECLINE when any additive denominator term contains a non-square radical. Implement a small predicate over collect_additive_terms(den): for each term, walk its multiplicative factors with the already-imported extract_root_base_and_index; if any factor is a root whose index != 2 (i.e., n>=3 nth-roots like 2^(1/3), 4^(1/3)), return None. Difference-of-squares is only sound/terminating for square-root towers, so this restores termination; the two existing pins stay green (test_generalized_rationalization_expands_denominator uses sqrt2/sqrt3 -> kept; test_cube_root_binomial_rationalization is a 2-term cube binomial handled by RationalizeNthRootBinomial, untouched). After the gate the 3 surd inputs return an honest unsimplified echo (a strict improvement over a hang; correct answer 2^(1/3)-1 is a NON-answer we simply don't produce). OPTIONAL follow-up (separate cycle) to actually output 2^(1/3)-1: add a closed-form cubic-norm rationalization for a+b*t+c*t^2 (t=k^(1/3)) reusing RationalizeNthRootBinomial's geometric-sum machinery — for the geometric form 1+t+t^2 multiply by (t-1) giving (t-1)/(k-1). The diff((sin(x)/x)^2,x,2) member is NOT fixed by this and is the residual multi-cycle work: the honest-decline path is a real (default) time/work deadline returning best-so-far, but that FIRST requires closing the deadline-plumbing gap found here (the existing cooperative deadline does not bound these hot loops), which touches every simplification (large huella) — do not rush.
- **Blast radius:** Surd gate (primary fix): very low. Scope is only fractions whose denominator is a >=3-term sum containing a non-square radical — those currently HANG (no valid output), so gating them to decline can only help. Both existing rationalization-stability pins (sqrt trinomial; cube binomial) are outside the gated set and remain green. One thing to sweep: confirm no eval/derive test currently DEPENDS on a >=3-term cube-root denominator being rationalized (impossible for a passing test today since it hangs) and that the sqrt-tower behavior at rationalize phase is unchanged. Diff-member / deadline part: broad — any change to the anti-worsen ratchet, the transform_expr_recursive halt, or a global default deadline touches the entire simplifier surface and must be validated against the full suite + guardrail/pressure benchmarks (memory: C5 orchestration fix is do-not-rush).
- **Maquinaria reutilizable:** extract_root_base_and_index (root_forms.rs:2552, already imported into rationalize_diff_squares_support.rs as extract_root_base) returns index=2 for sqrt vs 3 for k^(1/3) — directly usable for the gate. contains_irrational and collect_additive_terms_flat_add (already imported). The is_sqrt_root closure pattern (same file, lines 109-122 in try_rewrite_rationalize_denominator_diff_squares_expr) already restricts the binary diff-squares rule to exponent-denom==2 — mirror it for the n-ary case. RationalizeNthRootBinomial geometric-sum builder (same file, lines 165-275) is the reusable core for the optional cubic-norm correct-answer capability. For the orchestration side: existing deadline field (engine/transform/mod.rs:148 time_budget_exceeded, orchestrator.rs:25985 initialize_deadline_if_needed) and run_phase inter-iteration seen_hashes guard (orchestrator.rs:26256) are the hooks to extend, plus the IntraNode CycleDetector at rule_application.rs:288.
- **ROI:** Non-terminating simplification (depth_overflow at depth 51 that fails to halt, unbounded RSS) driven by inter-node expand↔factor oscillation that the IntraNode CycleDetector cannot see — the documented C5 hang. Triggers: cubic-surd denominator rationalization (generic conjugate multiplication explodes instead of the closed-form cubic-field norm) and repeated quotient-rule differentiation. A closed-form cubic-norm rationalization would cheaply defuse the 3 surd cases; the general oscillation is an orchestration fix (memory: do not rush). 4 P0 hangs.

### F12 [P2] complex-value-domain-non-real-dropped

- **Representante:** `solve(x^2 + x + 1 = 0, x)`
- **Correcto:** { -1/2 - (sqrt(3)/2)i, -1/2 + (sqrt(3)/2)i }  (under --value-domain complex)
- **Miembros (13):** `solve(e^x = -1, x)`; `solve(e^x = -2, x)`; `solve(e^x = -1/2, x)`; `solve(2^x = -4, x)`; `ln(-1)`; `ln(-2)`; `solve(x^3 = i, x)`; `solve(x^4 = i, x)`; `solve(x^6 = 1, x)`; `solve(x^8 = 1, x)`; `solve(x^4 = -4, x)`; `solve(x^2 + x + 1 = 0, x)`; `solve(x^3 = -8, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/quadratic_strategy.rs:126 (representative chokepoint); leaf emit at crates/cas_solver_core/src/solution_set.rs:529
- **Mecanismo:** Root cause: `--value-domain complex` -> `ValueDomain::ComplexEnabled` is threaded into the CONST-FOLD leaves (const_fold_local/folds/{sqrt,pow,imaginary}.rs) and into NARRATION/GATING (`is_real_only` reaches quadratic_didactic.rs:826, log_domain.rs:290 classify), but it never reaches the RESULT-GENERATING solve/eval leaves — those unconditionally run real-only algorithms.

Smoking gun (representative x^2+x+1=0): quadratic_strategy.rs `execute_quadratic_coefficient_pipeline_with_default_didactic_and_numeric_solution_with_state` receives `is_real_only` (line 78) and forwards it ONLY to the didactic pipeline (line 108-124). The actual solution comes from `execute_quadratic_coefficient_solve_plan_with_default_numeric_solution_with_state` (line 126-137) which is NOT given `is_real_only`; it flows to solution_set.rs `quadratic_numeric_solution` (line 449) whose `RelOp::Eq` arm returns `SolutionSet::Empty` when the discriminant is negative (line 529). So the flag is 'honored in reported semantics' (narration) but the solve stays real -> 'No solution'.

Same shape, different leaves, across the whole family:
- Polynomial/nth-root roots: rational_roots.rs + isolation_power.rs enumerate only real roots (x^3=-8 -> {-2}; x^6=1/x^8=1 -> {-1,1}; x^2=-4/x^4=-4 -> Empty) — no de Moivre enumeration, `is_real_only` ignored.
- exp/log solve (e^x=c<0, 2^x=-4): log_domain.rs `classify_log_solve_for_value_domain` returns `Ok` (proceed) when complex, but there is NO downstream complex-log family generator, so isolation rewrites x=ln(neg) which evaluates to Empty. `LogSolveDecision::NeedsComplex`->Residual fires only under wildcard+assume.
- ln(-1)/ln(-2) EVAL: the real value engine returns `Constant::Undefined` (const_eval.rs:185 sentinel); no complex-ln fold exists (folds/ only special-case (-1)^(1/2)->i and sqrt).
- x^3=i, x^4=i: const-fold leaves `i^(1/3)` unevaluated and solve never enumerates the n roots.
The single conceptual defect: value_domain is a real-only pipeline whose leaves have no non-real code path and whose plumbing (is_real_only) is consumed for gating/narration, not generation.
- **Boceto de fix:** Two tiers; pick per appetite.

TIER A — HONEST DECLINE (bounded-single-cycle, kills the WRONG-ANSWER nature family-wide, does not compute correct complex answers). Add one post-pass in the solve orchestrator and one gate in ln eval: when `value_domain == ComplexEnabled` and a leaf is about to emit a misleading result for something that provably has non-real solutions, emit `SolutionSet::Residual(solve(eq,var))` (echo unsolved) instead of `Empty`, and leave `ln(negative)` symbolic instead of `Undefined`. Concretely: (1) polynomial equations — FTA guarantees n complex roots, so an `Empty` under ComplexEnabled is always a capability gap -> Residual; (2) e^x=c / a^x=c with c<0 -> Residual; (3) generalize the existing `LogSolveDecision::NeedsComplex -> ReturnResidualInWildcard` (log_domain.rs:98) to fire under ComplexEnabled generally, not only wildcard+assume; (4) ln eval leaf: gate the `Constant::Undefined` sentinel on RealOnly. Reuses `SolutionSet::Residual` + `NeedsComplex` which already exist.

TIER B — FULL CAPABILITY (multi-cycle, the deferred F7/Fase-2 item). (1) Thread `is_real_only` into `quadratic_numeric_solution` (solution_set.rs:449) and the nth-root/rational-roots builders; when complex, emit i-bearing roots: quadratic -> -b/2a +/- i*sqrt(|Δ|)/2a; x^n=c -> de Moivre enumeration seeding on `cas_math::const_eval::try_eval_pow_literal` (already yields the PRINCIPAL complex root, e.g. (-8)^(1/3)->1+i*sqrt(3), (-4)^(1/2)->2i) times the n-th roots of unity e^(2πik/n). (2) exp/log: emit the family x = ln|c| + i*(arg(c)+2πk) (needs a representation choice — a new imaginary-Periodic variant or Discrete over k). (3) Complex ln eval fold: ln(z) -> ln|z| + i*arg(z) gated on ComplexEnabled. All results are representable via existing `SolutionSet::Discrete(Vec<ExprId>)` + `Constant::I`.
- **Blast radius:** Tier A (honest decline): LOW-to-MODERATE. Fires only when ComplexEnabled, which is NON-DEFAULT (default RealOnly), so the entire real-domain test corpus is untouched. Main risk is scoping the Empty->Residual post-pass so it never triggers in RealOnly and only for provable capability gaps (polynomials via FTA, neg-RHS exp/log); a naive 'Empty->Residual whenever complex' would wrongly echo genuinely-unsatisfiable systems. Touch points: solve orchestrator finalize + ln eval sentinel gate (~2-4 sites).

Tier B (full capability): MODERATE-to-HIGH and genuinely multi-cycle. Introducing complex ExprIds into `SolutionSet::Discrete` propagates into layers that assume a REAL total order and real simplification: quadratic root construction sorts via `order_pair_by_value`/`compare_values` (quadratic_formula.rs:246, solution_set.rs:39/282) — undefined on ℂ, must be bypassed for complex; interval/union machinery is real-only; the simplifier folds |z|^2->z^2 and i^2->-1 (must be verified consistent); formatters, proof/verification, and de-dup all assume real. exp/log needs a new solution representation for the +2πk imaginary family. This cross-layer reach is exactly why it was deferred to Fase-2.
- **Maquinaria reutilizable:** 1) `SolutionSet::Discrete(Vec<ExprId>)` + `cas_ast::Constant::I` (expression.rs:56) — complex roots are already REPRESENTABLE; no new SolutionSet variant needed for the polynomial cases. 2) `cas_math::const_eval::try_eval_pow_literal` already computes the PRINCIPAL complex root of a negative/complex base to a fractional power (verified: (-8)^(1/3)->1+i*sqrt(3), (-4)^(1/2)->2i via const_fold_local/folds/pow.rs + sqrt.rs) — the seed for de Moivre enumeration. 3) `SolutionSet::Residual(ExprId)` (domain.rs:364, purpose-built for 'complex log needed but in RealOnly') + `LogSolveDecision::NeedsComplex`/`ReturnResidualInWildcard` (log_domain.rs:45,98) — the honest-echo path already exists, just needs its firing condition widened from wildcard+assume to ComplexEnabled. 4) `is_real_only`/`value_domain_real_only` plumbing already reaches the quadratic strategy (quadratic_strategy.rs:78), log-solve tactics (solve_runtime_flow_strategy_log.rs:30), and pow kernels (strategy_options.rs) — the wiring to REACH every leaf exists; only the leaves ignore it. 5) `ValueDomain::is_complex_enabled()` (value_domain.rs:25) helper already present.
- **ROI:** With --value-domain complex the flag is honored in the reported semantics but the actual solve/eval DISPATCH still runs real-domain algorithms, which never emit non-real results. So degree-n polynomials return only real/principal roots (or ∅), and log/exp of negative reals return ∅ or the leaked real-domain 'undefined' sentinel instead of ln|z|+i·arg / the x=ln|c|+iπ(2k+1) family. Largest breadth (13) and one conceptual root, but a genuine capability build spanning complex root enumeration and complex log — the deferred F7/Fase-2 item. High reuse, high cost, capability-shaped.

### F13 [P1] bigint-power-eager-materialization

- **Representante:** `10^50000000`
- **Correcto:** the 50,000,001-digit integer 10^50000000 (retain symbolically / stream digits); must not hang
- **Miembros (2):** `2^100000000`; `10^50000000`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_math/src/power_eval_support.rs:161 (root-extraction block entered for integer exponents); enabled by extract_root_factor at crates/cas_math/src/root_forms.rs:2680
- **Mecanismo:** For `base^exp` with an INTEGER exponent > 1000, `EvaluatePowerRule` (crates/cas_engine/src/rules/exponents/power_rules.rs:535) first calls `try_rewrite_literal_power_eval_expr` → `const_eval::try_eval_pow_literal`, which correctly DECLINES because of the `MAX_ABS_POW = 1000` guard (const_eval.rs:19,37). Control then falls to `try_rewrite_evaluate_power_expr` (power_eval_support.rs:150), a ROOT-extraction routine never meant for integer exponents. An integer exponent has `denom = 1`, so `n = denom.to_u32() = 1` (line 161) and the block runs, calling `extract_root_factor(base, 1)` (root_forms.rs:2629). With k=1 that routine splits the base into a perfect-power part put OUTSIDE and the trailing cofactor dumped INSIDE at line 2680: for base 10 it yields outside=2, inside=5 (base 2 → outside=2,inside=1; odd-prime bases → outside=1). It then EAGERLY MATERIALIZES the outside factor as a full exact BigInt via `num_traits::Pow::pow` (line 175) — e.g. `2^1001` (302 digits), or `2^50000000` (~15M digits) for the representative — an O(n^2) big-int build plus O(n^2) decimal base-conversion at display = de-facto hang. The inside cofactor is left as symbolic `Pow(5, exp)`. Two defects: (1) WRONG non-canonical output `2^exp·5^exp` instead of the integer `10^exp` (equal but a partial un-evaluated form the engine treats as normal), and (2) eager materialization → hang. Verified vs ground truth: CLI `10^1001` = exactly `2^1001·5^1001`, `10^2000` = `2^2000·5^2000`, `2^20000000` times out at 30s. NOTE: the ROI's 'f64-contaminated above ~1024' hypothesis is FALSE — the materialized digits are exact BigInt (byte-for-byte equal to Python `2^1001`); the real defects are the wrong partial form + astronomical exact materialization. Odd-prime bases (7, 3, ...) already escape this path (outside=1 → guard at line 168 fails → None → symbolic, 0.06s), which is exactly the correct target for all integer powers.
- **Boceto de fix:** Single chokepoint: restrict `try_rewrite_evaluate_power_expr` to GENUINE roots. After `if let Some(n) = denom.to_u32()` at power_eval_support.rs:161, add `if n < 2 { return None; }` (equivalently: bail when the exponent is an integer / denom == 1). Then integer exponents > 1000 decline on BOTH paths and remain symbolic `Pow(base, exp)` — matching the family's accepted 'retain symbolically; must not hang', with zero materialization. Also closes negative huge integer exponents (`10^(-1001)`) and every composite/even base (2,4,6,8,10,...) in one edit. The MAX_ABS_POW-guarded `try_eval_pow_literal` still fully evaluates |exp| ≤ 1000, so nothing ≤1000 regresses. CAVEAT: the JSON's 'multi-cycle' label is accurate only for the ambitious ROI reading (raise MAX_ABS_POW + add a non-f64 big-int power/streaming decimal path that actually PRODUCES the 50M-digit integer). That is genuinely multi-cycle but NOT required for soundness — the spec permits symbolic retention — so the bounded one-guard fix fully closes the wrong-answer + hang family; digit-production is a separate optional enhancement.
- **Blast radius:** Very low. The guard only affects integer exponents in (1000, i32::MAX] whose base has a factor of 2 or a small perfect-power factor — cases that TODAY already emit a wrong partial `2^n·...` form or hang, so declining to symbolic is strictly better. Fractional-exponent roots (denom ≥ 2) untouched; all existing tests of this function use fractional exponents (`8^(1/3)`→2, `0^(-1/2)`→undefined, power_eval_support.rs:259,286). No test pins the large-integer split form (grep found none). Sole caller is EvaluatePowerRule (power_rules.rs:547). `const_fold::fold_pow` (helpers.rs:209) already delegates only to the guarded `try_eval_pow_literal`, so no parallel leak — this is a true chokepoint.
- **Maquinaria reutilizable:** MAX_ABS_POW guard + `const_eval::try_eval_pow_literal` (const_eval.rs:19,37) is the already-correct guarded evaluator that keeps huge powers symbolic; the root path just needs to stop shadowing it for integers. The odd-prime-base decline (`7^50000000` → symbolic, 0.06s) already demonstrates the exact post-fix behavior. `extract_root_factor` stays intact for genuine roots. Reproduction harness: `target/release/cas_cli eval "..." --steps on` plus Python BigInt ground-truth diff.
- **ROI:** Bare integer powers with astronomically large exponents are eagerly fully materialized via O(n^2) decimal conversion → de-facto hang, and the >1000-exponent fast path is itself buggy (10^n evaluated as 2^n·5^n with 5^n left symbolic; 2^n decimal digits f64-contaminated above ~1024). Fix: a size threshold that keeps huge powers symbolic plus a correct big-int (non-f64) power/base-conversion path. Same missing 'size guard on power materialization' trigger as the modexp family. Lower breadth (2), mixes perf and latent soundness.

### F14 [P1] rational-surd-denominator-root-extraction

- **Representante:** `solve((x^3-2*x)/(x-sqrt(2))=0, x)`
- **Correcto:** {0, -sqrt(2)}  (x=sqrt(2) is excluded — it zeroes the denominator)
- **Miembros (1):** `solve((x^3-2*x)/(x-sqrt(2))=0, x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:10505 (insertion point in solve_local_core_inner, immediately after the equation_is_nonzero_const_over_polynomial gate at 10502-10506). Root mechanism originates at crates/cas_solver_core/src/isolation_arithmetic.rs:1474-1493 (DivIsolationRoute::VariableInNumerator) + crates/cas_solver_core/src/equation_rewrite.rs:127 (isolate_div_numerator).
- **Mecanismo:** There is NO top-level rational-EQUATION handler (only try_solve_rational_constant_inequality for inequalities and equation_is_nonzero_const_over_polynomial for a CONSTANT numerator). So a rational equation N/D = 0 with a polynomial numerator falls through every early handler in solve_local_core_inner to the generic Div-isolation path. That path (isolation_arithmetic.rs:1474, VariableInNumerator route) calls isolate_div_numerator (equation_rewrite.rs:127) to rewrite N/D = 0 as N = 0·D, then recursively solves N = 0 via the isolation-only continuation `solve_isolate` — NOT the full solve pipeline. That continuation has linear and quadratic strategies but NO polynomial factoring / zero-product / biquadratic strategy, so for a degree ≥ 3 numerator it isolates the leading power: x^3 − 2x = 0 → x^3 = 2x → cube-root both sides → x = (2x)^(1/3) → residual echo `solve(x − (2·x)^(1/3) = 0, x)`. This drops ALL roots (the real answer x=0 and x=−√2 vanish). Proven: standalone solve(x^3−2x=0), solve(x^3=2x), and even solve(x^3−2x = 0*(x-1)) all factor correctly at the top level and are fast, but the SAME numerator handed to the div-numerator recursion nth-root-isolates. Boundary confirmed empirically: degree-2 numerators work (constant RHS after moving → sqrt gives both roots), factorable/irrational cubics and biquadratics all fail identically (x^3−4x, x^3−6x^2+11x−6, x^4−5x^2+4 all emit the (…)^(1/n) garbage), and denominators that divide the numerator (/x, or x−1 into x^3−x) work because the simplifier cancels to a plain polynomial first. The surd variant (x−√2) additionally near-hangs ~29s because the generic path repeatedly simplifies/rationalizes the surd-affine denominator through its conjugate before the isolation fails; the RAW-tree const-numerator gate (solve(3/(x−√2)=0) → No solution in 0.01s) already demonstrates that a RAW early handler bypasses that cost.
- **Boceto de fix:** Add a top-level rational-equation handler try_solve_rational_equation(simplifier, eq, var) -> Option<SolutionSet> in solve_backend_local.rs and call it in solve_local_core_inner right after the nonzero-const gate (~line 10505), before generic isolation. Body: (1) require eq.op == Eq and eq.rhs provably rational 0; (2) on the RAW lhs tree (peel Neg, exactly like equation_is_nonzero_const_over_polynomial lines 458-469 — do NOT simplify first, to avoid the surd-conjugate rationalization and the near-hang) match Div(N, D); (3) require N to be a polynomial in var via Polynomial::from_expr(ctx, N, var).is_ok() and require D to contain var (decline for a constant/degree-0 denominator — that is the ordinary path's job); (4) solve N = 0 with the FULL entry via the existing solve_relation_set(simplifier, var, N, zero, RelOp::Eq) (it recurses through solver_entrypoints_solve::solve, which factors x·(x^2−2) and returns {0, √2, −√2} in ~0.02s); (5) for each candidate root r in the returned Discrete set, EXCLUDE it iff it is a pole: substitute r into D only (cas_solver_core::substitution::substitute_named_var), simplify, and test exact zero with as_rational_const == 0 — do NOT simplify N/D and evaluate, which would cancel removable poles and wrongly keep them; if any root's pole-membership is undecidable (D(r) neither provably 0 nor provably nonzero), return None to preserve prior behavior honestly; (6) return the filtered Discrete set. For the representative this yields {0, −√2} (√2 excluded because √2−√2 simplifies to exact 0). This is a shared chokepoint that closes the whole degree ≥ 3 family and both symptoms (wrong answer + near-hang) at once, and is strictly more correct for the degree-2 / cancelling cases that already work (they produce the same answers). For full coverage of a rational factor nested inside a product, the identical check can also be added to the recursive solve_inner in solve_core_runtime.rs (per the repo's recurring "top-level AND solve_inner" lesson), but that is not required for this single-member family.
- **Blast radius:** Low. The handler only fires for Div(N,D)=0 with a genuine polynomial numerator and a var-containing denominator — a shape the generic path currently mishandles (degree ≥ 3) or handles more slowly (degree ≤ 2, which the new handler reproduces identically). It declines (returns None) whenever N is not a plain polynomial or any pole-membership is undecidable, so no new wrong answers. No infinite recursion: N=0 is a pole-free polynomial and re-enters no rational branch. Regression check: run the full workspace test suite plus spot-check the currently-working rational forms — (x^3−x)/(x-1) → {-1,0}, (x^2-2)/(x-1) → {±√2}, (x^2-3x+2)/(x-10) → {1,2}, 3/(x-√2) → No solution — and confirm the exact-zero pole test never drops a genuine non-pole root (guard with provably-nonzero, keep-on-unknown).
- **Maquinaria reutilizable:** solve_relation_set (solve_backend_local.rs:1062, recurses through the full solver — factors the numerator); the RAW Neg-peel + Div match pattern in equation_is_nonzero_const_over_polynomial (solve_backend_local.rs:458-469); cas_math::polynomial::Polynomial::from_expr for the numerator-is-polynomial gate; cas_math::numeric_eval::as_rational_const for the exact zero test; cas_solver_core::substitution::substitute_named_var (substitution.rs:846) for pole substitution; simplifier.simplify for reducing D(r). The inequality sibling try_solve_rational_constant_inequality (solve_backend_local.rs:1228) is the structural template (RAW split, keep-denominator-uncancelled discipline, verify-before-return). check_root (line 206) is NOT sufficient for surd poles (rational-only f64) — use the explicit exact D(r)=0 test instead.
- **ROI:** Narrow but severe single instance: a trivial rational equation with a surd denominator both near-hangs (~29.5s across runs) and emits a non-faithful garbage residual — it rewrites the numerator x^3−2x=0 as x=(2x)^(1/3) (cube-rooting both sides of x^3=2x), a lossy transform that drops the real root −sqrt(2), rather than factoring x(x^2−2) and excluding the pole x=sqrt(2). Fix: prefer factorization over nth-root extraction for x^n = k·x forms and honor the denominator pole set. Low breadth; clean concrete dropped-root plus near-hang.

## Confirmados por frente


**abs-deep** (8):
- `solve(abs(abs(x)-abs(x-2))=1, x)` → `{ 3/2 }` | correcto: {1/2, 3/2}
- `solve(abs(abs(x)-abs(x-2))=2, x)` → `[2, infinity) if |x - 2| + 2 >= 0` | correcto: (-∞, 0] ∪ [2, ∞)  (i.e. x ≤ 0 or x ≥ 2)
- `solve(abs(2*abs(x)-abs(x-1))=1, x)` → `{ 0 }` | correcto: {-2, 0, 2/3}
- `solve(abs(abs(2*x)-abs(x-3))=1, x)` → `{ -2, 2/3 }` | correcto: { -4, -2, 2/3, 4/3 }
- `solve(abs(abs(x)-2)/abs(x)=1, x)` → `{ -1 }` | correcto: {-1, 1}
- `solve(abs(abs(x)-3)/abs(x)=1, x)` → `{ -3/2 }` | correcto: {-3/2, 3/2}
- `solve(abs(abs(x-2)-abs(x+2))=2, x)` → `{ 1 }` | correcto: {-1, 1}
- `solve(abs(abs(x-1)-abs(x-3))=1, x)` → `{ 3/2 }` | correcto: {3/2, 5/2}

**complex-domain** (7):
- `solve(x^3 = i, x)` [--value-domain complex] → `{ i^(1/3) }  (JSON result field "{ i^(1/3) }"; LaTeX \left\{ \sqrt[3]{` | correcto: In the complex value domain, x^3 = i has exactly three roots: { sqrt(3)/2 + i/2, -sqrt(3)/2 + i/2, -
- `solve(x^4 = i, x)` [--value-domain complex] → `{ i^(1/4), -(i^(1/4)) }  (result_latex: \left\{ \sqrt[4]{i}, -\sqrt[4]` | correcto: In the complex value domain, x^4 = i has FOUR roots — the four 4th-roots of i = e^(iπ/2), all on the
- `solve(x^6 = 1, x)` [--value-domain complex] → `result: "{ -1, 1 }" (result_latex "\\left\\{ -1, 1 \\right\\}"), ok:tr` | correcto: The six sixth-roots of unity: { 1, -1, 1/2 + (√3/2)i, 1/2 - (√3/2)i, -1/2 + (√3/2)i, -1/2 - (√3/2)i 
- `solve(x^8 = 1, x)` [--value-domain complex] → `{ -1, 1 }` | correcto: { 1, -1, i, -i, sqrt(2)/2 + sqrt(2)/2*i, sqrt(2)/2 - sqrt(2)/2*i, -sqrt(2)/2 + sqrt(2)/2*i, -sqrt(2)
- `solve(x^4 = -4, x)` [--value-domain complex] → `{"ok":true,"result":"No solution","result_latex":"\\emptyset", semanti` | correcto: In the complex value domain (--value-domain complex): x ∈ {1+i, 1−i, −1+i, −1−i} = {±1 ± i}. (Equiva
- `solve(x^2 + x + 1 = 0, x)` [--value-domain complex] → `"No solution" (result_latex: \emptyset), with semantics.value_domain =` | correcto: In the complex value domain (as selected by --value-domain complex), x^2 + x + 1 = 0 has exactly two
- `solve(x^3 = -8, x)` [--value-domain complex] → `{ -2 }  (semantics.value_domain = "complex")` | correcto: { -2, 1 - sqrt(3)*i, 1 + sqrt(3)*i }

**derivatives-deep** (7):
- `diff(integrate(x*sin(t^2), t, 0, 1), x)` → `0` | correcto: integrate(sin(t^2), t, 0, 1)  (= sqrt(2)*sqrt(pi)*fresnels(sqrt(2)/sqrt(pi))/2 ≈ 0.310268301723381),
- `diff(integrate(1/(t+x), t, 0, 1), x)` → `result": "0" (result_latex "0"); the inner definite integral integrate` | correcto: 1/(x+1) - 1/x = -1/(x*(x+1)) = -1/(x^2 + x)
- `diff(integrate(sin(x*t)/t, t, 1, x), x)` → `sin(x^2) / x  (from target/release/cas_cli eval "diff(integrate(sin(x*` | correcto: (2*sin(x^2) - sin(x))/x
- `diff(integrate(exp(x*t^2), t, 0, x), x)` → `e^(x^3)` | correcto: e^(x^3) + integral_0^x t^2*e^(x*t^2) dt  =  (3/2)*e^(x^3) - (1/(2x))*integral_0^x e^(x*t^2) dt  =  (
- `diff(integrate(exp(-t^2)*cos(x*t), t, 0, oo), x)` → `undefined  (JSON: "ok": true, "result": "undefined", "result_latex": "` | correcto: -sqrt(pi)*x*exp(-x^2/4)/4   (equivalently -1/4·x·pi^(1/2)·e^(-x^2/4)). Because integrate(exp(-t^2)*c
- `diff(integrate(1/(t^2+x), t, 0, oo), x)` → `undefined` | correcto: -pi/(4*x^(3/2))  (equivalently -π/(4·√(x³)) ), for x > 0
- `diff((sin(x)/x)^2, x, 2)` → `HANG. `target/release/cas_cli eval "diff((sin(x)/x)^2, x, 2)" --format` | correcto: f''(x) for f = (sin(x)/x)^2 is:  2·(x²·cos(2x) − 2x·sin(2x) + 3·sin²(x)) / x⁴,  equivalently  (2·(co

**exp-log-deep** (6):
- `solve(e^x = -1, x)` [--value-domain complex] → `No solution (result: "No solution", result_latex: "\emptyset"), with s` | correcto: x = iπ(2k+1) for k ∈ ℤ  (equivalently x = iπ + 2πik). Concrete members: …, −3iπ, −iπ, iπ, 3iπ, 5iπ, 
- `solve(e^x = -2, x)` [--value-domain complex] → `{"ok": true, "result": "No solution", "result_latex": "\\emptyset", "s` | correcto: In the complex value domain, e^x = -2 has the infinite solution family x = ln(2) + i·π·(2k+1), k ∈ ℤ
- `solve(e^x = -1/2, x)` [--value-domain complex] → `{"result": "No solution", "result_latex": "\\emptyset", "ok": true, se` | correcto: Under --value-domain complex: x = -ln(2) + i·π·(2n+1), n ∈ ℤ  (equivalently ln(1/2) + i(2n+1)π). Pri
- `solve(2^x = -4, x)` [--value-domain complex] → `result: "No solution" (result_latex: "\emptyset"), ok: true, with sema` | correcto: In the complex value domain, 2^x = -4 has the solution family x = 2 + iπ(2k+1)/ln(2) for k ∈ ℤ. Prin
- `ln(-1)` [--value-domain complex] → `undefined` | correcto: i*pi  (principal-branch complex logarithm: ln(-1) = ln|-1| + i*arg(-1) = 0 + i*pi = i*pi; sympy sp.l
- `ln(-2)` [--value-domain complex] → `undefined` | correcto: ln(-2) = ln(2) + i·pi  (principal branch), numerically 0.693147180559945 + 3.14159265358979·i

**identities-simplify-deep** (3):
- `1/(1+2^(1/3)+4^(1/3))` → `HANG / non-termination. `target/release/cas_cli eval "1/(1+2^(1/3)+4^(` | correcto: 2^(1/3) - 1  (≈ 0.259921049894873164767210607278). Since 1/(1 + 2^(1/3) + 4^(1/3)) = 1/(1 + a + a^2)
- `1/(1+3^(1/3)+9^(1/3))` → `No result — the engine HANGS. It emits only a stderr WARN "depth_overf` | correcto: (3^(1/3) - 1)/2  =  (cbrt(3) - 1)/2  ≈ 0.221124785153704191160819
- `1/(a+b*2^(1/3)+c*4^(1/3))` → `HANG. `target/release/cas_cli eval "1/(a+b*2^(1/3)+c*4^(1/3))" --forma` | correcto: 1/(a+b·2^(1/3)+c·4^(1/3)) = [ (a^2-2bc) + (2c^2-ab)·2^(1/3) + (b^2-ac)·4^(1/3) ] / (a^3 + 2b^3 + 4c^

**inequalities-deep** (8):
- `solve(abs(x-a) < 3, x)` → `"No solution" (result_latex: \emptyset). JSON: ok=true, result="No sol` | correcto: The open interval (a - 3, a + 3), i.e. {x : a - 3 < x < a + 3}. Since |x-a| < 3 ⟺ -3 < x-a < 3 ⟺ a-3
- `solve(abs(x-a) <= 3, x)` → `[a - 3, a - 3] U [a + 3, a + 3]` | correcto: The closed interval [a-3, a+3], i.e. {x : a-3 <= x <= a+3}. Equivalently a-3 <= x <= a+3. Sympy solv
- `solve(abs(x-a) > 3, x)` → `Result: "All real numbers" (result_latex: \mathbb{R}); ok=true, real/g` | correcto: x < a - 3 or x > a + 3, i.e. the set (-∞, a-3) ∪ (a+3, ∞).
- `solve((x+a)*(x-a) > 0, x)` → `(-infinity, a) U (-a, infinity)` | correcto: (-∞, -|a|) ∪ (|a|, +∞), i.e. x < -|a| or x > |a|. Equivalently x² > a². For a<0 this equals (-∞, a) 
- `solve((x-1)*(x-a) > 0, x)` → `(-infinity, 1) U (a, infinity)` | correcto: Case-split on the ordering of the roots {1, a}: for a > 1, (-inf, 1) U (a, inf); for a = 1, (-inf, 1
- `solve((x-2)*(x-a) < 0, x)` → `(2, a)  (JSON result field: "result": "(2, a)", result_latex "\\left(2` | correcto: The solution is the open interval strictly between the two roots 2 and a. Since the order of 2 and a
- `solve((x-a)*(x-b)*(x-c) > 0, x)` → `(a, b) U (c, infinity)   [required_conditions: [], warnings: []]` | correcto: a, b, c are FREE real symbols with no ordering. Let r1 <= r2 <= r3 be the sorted roots of {a,b,c}. S
- `solve((x-a)^2*(x-b) > 0, x)` → `(b, infinity)` | correcto: (b, infinity) \ {a}, i.e. x > b AND x != a. Equivalently: if a <= b the set is (b, infinity); if a >

**matrices-deep** (8):
- `[[1,1],[1,0]]^14` → `A ~400KB unsimplified/mangled expression tree emitted with a `depth_ov` | correcto: [[610, 377], [377, 233]]
- `[[2,0,0],[0,1,0],[0,0,1]]^30` → `HANG — `target/release/cas_cli eval "[[2,0,0],[0,1,0],[0,0,1]]^30" --f` | correcto: [[1073741824, 0, 0], [0, 1, 0], [0, 0, 1]]  (i.e. diag(2^30, 1, 1), with 2^30 = 1073741824)
- `[[2,0],[0,3]]^20` → `A ~32MB unevaluated/uncombined expression tree. Each matrix entry is e` | correcto: [[1048576, 0], [0, 3486784401]]  (i.e. diag(2^20, 3^20))
- `[[1,2],[3,4]]^15` → `ok=true; result is a truncated 658530-char (result_chars=658530, resul` | correcto: [[21407570533, 31199973606], [46799960409, 68207530942]]
- `[[1,1,0],[0,1,1],[0,0,1]]^10` → `ok=true, but the `result` field is garbage: a 176,466-char truncated (` | correcto: [[1, 10, 45], [0, 1, 10], [0, 0, 1]]
- `[[1,1],[1,0]]^-14` → `ok:true, but result is a 157,017-char uncombined expression tree. Matr` | correcto: [[233, -377], [-377, 610]]
- `trace([[1,1],[1,0]]^30)` → `Hang / timeout with no result (exit 124 at 60s and 150s); emits depth_` | correcto: 1860498
- `det([[1,2],[3,4]]^20)` → `Hang: 60s timeout (exit 124), empty stdout, 28.5 MB of depth_overflow ` | correcto: 1048576

**number-theory-deep** (5):
- `mod(6^50000000, 100)` → `HANG (no result produced). Re-ran `target/release/cas_cli eval "mod(6^` | correcto: 76
- `mod(123456789^987654321, 1000000007)` → `HANG (exit 124, no output) on the exact input` | correcto: 652541198
- `mod(10^100000000, 7)` → `HANG / timeout — no output. Ran `target/release/cas_cli eval "mod(10^1` | correcto: 4
- `2^100000000` → `Timeout / hang: `eval "2^100000000" --format json` produces ZERO bytes` | correcto: 2^100000000 is the exact 30,103,000-digit integer approximately 3.684665936980458763... x 10^3010299
- `10^50000000` → `HANG. `target/release/cas_cli eval "10^50000000" --format json` produc` | correcto: 10^50000000 is the integer '1' followed by 50,000,000 zeros (a 50,000,001-digit number); equivalentl

**radicals-deep** (2):
- `solve(sqrt(a-x)=x, x)` → `{ 1/2·((4·a + 1)^(1/2) - 1), 1/2·(-(4·a + 1)^(1/2) - 1) }  with requir` | correcto: x = (sqrt(4*a+1) - 1)/2, i.e. { (sqrt(4a+1)-1)/2 }, valid for a >= 0 (real domain). The other algebr
- `solve(sqrt(a-x)=x-1, x)` → `{ 1/2·((4·a - 3)^(1/2) + 1), 1/2·(1 - (4·a - 3)^(1/2)) }  [required: a` | correcto: Real domain, symbolic parameter a. Squaring sqrt(a-x)=x-1 gives x^2 - x + (1-a) = 0, roots x = (1 ± 

**regression-recent** (4):
- `solve(1/cos(x)^2=1, x)` → `{ pi, 0 }` | correcto: x = n·π for all integers n, i.e. { k·pi : k ∈ ℤ } = {..., -2π, -π, 0, π, 2π, ...}
- `solve(1/sin(x)^2=1, x)` → `{ 1/2·pi, -1/2·pi }  (result_latex: \left\{ \frac{\pi}{2}, -\frac{1}{2` | correcto: x = π/2 + kπ, k ∈ ℤ  (equivalently { π/2 + kπ } = { …, -3π/2, -π/2, π/2, 3π/2, 5π/2, … }). From 1/si
- `solve(1/tan(x)^2=2, x)` → `solve(cos(x) - sin(x)·sqrt(2) = 0, x)  (with required conditions sin(x` | correcto: x = arctan(√2/2) + nπ  or  x = −arctan(√2/2) + nπ  (equivalently x = nπ ± arctan(1/√2)), for n ∈ ℤ. 
- `solve(1/tan(x)^2-1=0, x)` → `solve(cos(x) - 1·(sin(x)^2)^(1 / 2) = 0, x) if (sin(x)^2)^(1 / 2) >= 0` | correcto: x = π/4 + nπ/2, n ∈ ℤ  (equivalently {π/4 + nπ} ∪ {3π/4 + nπ}; i.e. ±π/4, ±3π/4, ±5π/4, ... — tan(x)

**series-deep** (6):
- `taylor(sin(x)/x, x, 0, 4)` → `result: "undefined" (leaked sentinel), with required_condition x ≠ 0; ` | correcto: 1 - x^2/6 + x^4/120  (equivalently x^4/120 - x^2/6 + 1)
- `taylor(x/(exp(x)-1), x, 0, 4)` → `result: "undefined" (ok:true), with required_condition NonZero on e^x ` | correcto: 1 - x/2 + x^2/12 - x^4/720  (i.e. 1 - (1/2)x + (1/12)x^2 + 0*x^3 - (1/720)x^4)
- `taylor((exp(x)-1)/x, x, 0, 4)` → `undefined` | correcto: 1 + x/2 + x^2/6 + x^3/24 + x^4/120  (Maclaurin series of (exp(x)-1)/x to order 4; equivalently (x^4 
- `taylor((1-cos(x))/x^2, x, 0, 4)` → `undefined  (JSON: "result": "undefined", with required condition x ≠ 0` | correcto: 1/2 - x^2/24 + x^4/720  (equivalently x^4/720 - x^2/24 + 1/2)
- `taylor(tan(x)/x, x, 0, 4)` → `undefined  (JSON: "result": "undefined", with required_conditions cos(` | correcto: 1 + x^2/3 + 2*x^4/15  (i.e. 1 + (1/3)x^2 + (2/15)x^4)
- `taylor(sinh(x)/x, x, 0, 4)` → `undefined` | correcto: 1 + x^2/6 + x^4/120

**solve-poly-deep** (1):
- `solve((x^3-2*x)/(x-sqrt(2))=0, x)` → `solve(x - (2·x)^(1 / 3) = 0, x)  [with required condition x - sqrt(2) ` | correcto: x = 0 or x = -sqrt(2). Solution set {0, -sqrt(2)}. The numerator x^3-2x = x(x^2-2) has roots 0, sqrt

**systems-multivar** (1):
- `solve(sqrt(x)+3=y, x)` → `{ (y - 3)^2 } with required condition "x ≥ 0" only (missing y ≥ 3). ok` | correcto: x = (y-3)^2 subject to y ≥ 3 (equivalently y-3 ≥ 0); for y < 3 there is no real solution. So the rea

**transcendental-mixed** (8):
- `solve(csc(x)^2>2, x)` → `result: "(-1/4·pi, 1/4·pi)" — the single bounded open interval (−π/4, ` | correcto: Solution = { x : |sin x| < √2/2 and sin x ≠ 0 } = ⋃_{k∈ℤ} ( (kπ − π/4, kπ) ∪ (kπ, kπ + π/4) ), i.e. 
- `solve(csc(x)^2<2, x)` → `(-infinity, -1/4·pi) U (1/4·pi, infinity)` | correcto: x ∈ ⋃_{k∈ℤ} (π/4 + kπ, 3π/4 + kπ). Equivalently, csc(x)²<2 ⟺ 1/sin²(x)<2 ⟺ sin²(x)>1/2 ⟺ |sin(x)|>√2
- `solve(sec(x)^2>2, x)` → `No solution (result: "No solution", result_latex: \emptyset). Same "No` | correcto: The real-domain solution of sec(x)^2 > 2 is a non-empty infinite periodic family of open intervals. 
- `solve(sec(x)^2<2, x)` → `result: "All real numbers" (result_latex "\\mathbb{R}"). Full JSON ok:` | correcto: Union over all integers k of the open intervals (k*pi - pi/4, k*pi + pi/4). Equivalently: sec(x)^2 <
- `solve(tanh(x)^2<1, x)` → `Result: "No solution" (result_latex: \emptyset). ok=true, real value d` | correcto: All real numbers: x ∈ ℝ, i.e. (-∞, ∞). Since tanh maps ℝ onto the open interval (-1, 1), |tanh(x)| <
- `solve(sec(x)^2=1, x)` → `{ pi, 0 }` | correcto: x = n·π, n ∈ ℤ  (equivalently { k·pi : k ∈ ℤ }). Derivation: sec(x)^2=1 ⟺ cos(x)^2=1 ⟺ sin(x)=0 ⟺ x=
- `solve(abs(csc(x))<2, x)` → `{"ok": true, "result": "No solution", "result_latex": "\\emptyset"} — ` | correcto: |csc(x)| < 2  ⟺  1/|sin(x)| < 2  ⟺  |sin(x)| > 1/2. Solution (real domain): the periodic union of op
- `solve(asin(x)+acos(x)=pi/2, x)` → `{"ok": true, "result": "All real numbers", "result_latex": "\\mathbb{R` | correcto: [-1, 1] (the closed interval, i.e. all real x with -1 <= x <= 1). The equation asin(x)+acos(x)=pi/2 

**trig-deep** (7):
- `solve(sec(x)^3=8, x)` → `{ 1/3·pi }  (from `./target/release/cas_cli eval "solve(sec(x)^3=8, x)` | correcto: x = ±π/3 + 2πk, k ∈ ℤ. Equivalently {π/3 + 2πk : k∈ℤ} ∪ {5π/3 + 2πk : k∈ℤ}. (Real domain: the only r
- `solve(csc(x)^3=1, x)` → `{ 1/2·pi }  (result_latex: \left\{ \frac{\pi}{2} \right\}) — a finite ` | correcto: x = π/2 + 2πk, k ∈ ℤ  (equivalently sin(x) = 1). In engine notation: { 1/2·pi + k·2·pi : k ∈ ℤ }.
- `solve(sec(x)^3=-8, x)` → `{ 2/3·pi }  (JSON result field: "{ 2/3·pi }", a single discrete value ` | correcto: x = 2π/3 + 2πn  OR  x = 4π/3 + 2πn,  n ∈ ℤ  (equivalently cos(x) = −1/2, i.e. x = ±2π/3 + 2πn). With
- `solve(sec(x)^5=32, x)` → `{ 1/3·pi }` | correcto: x = π/3 + 2kπ  or  x = 5π/3 + 2kπ  (equivalently x = ±π/3 + 2kπ), k ∈ ℤ. In the engine's own notatio
- `solve(sec(x)^3=2*sqrt(2), x)` → `{ arccos((1/2)^(1/3)·2^(-1/6)) }  — which simplifies exactly to arccos` | correcto: x = π/4 + 2πk  or  x = −π/4 + 2πk  (equivalently 7π/4 + 2πk), k ∈ ℤ. Derivation: sec(x)^3 = 2√2 > 0 
- `solve(csc(x)^3=8, x)` → `{ 1/6·pi }   (LaTeX: \left\{ \frac{\pi}{6} \right\}) — a finite discre` | correcto: x = pi/6 + 2*pi*n  OR  x = 5*pi/6 + 2*pi*n, for all integers n. (Since csc(x)^3=8 => csc(x)=2 => sin
- `solve(cot(x)+cot(2*x)=0, x)` → `{ 1/3·pi }  (JSON result field "{ 1/3·pi }", LaTeX \left\{ \frac{\pi}{` | correcto: x = pi/3 + n*pi  OR  x = 2*pi/3 + n*pi, n in Z (equivalently x = ±pi/3 mod pi). In (0, 2pi): {pi/3, 
