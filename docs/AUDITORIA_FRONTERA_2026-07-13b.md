# Auditoría de frontera 2026-07-13b (segunda del día)

Frontier-audit multiagente #2 del 2026-07-13, HEAD `20fbe3c45` (tras cerrar las 9 P0/P1 del audit 2026-07-13a). **19 frentes, 184 agentes, 78 candidatos → 58 DOBLE-CONFIRMADOS (refuter + matemático independiente), 8 familias.** Verificación 2-lentes con sympy + sustitución; oráculo independiente por hallazgo.

Sigue a [AUDITORIA_FRONTERA_2026-07-13.md](AUDITORIA_FRONTERA_2026-07-13.md) (9/9 P0/P1 cerradas). Metodología: 19 scouts read-only (uno por frente) → dedup → 2 lentes adversariales independientes por candidato (doble-CONFIRMED) → síntesis por familia de causa-raíz → root-cause read-only con file:line.

## Checklist de cierre (vivo)

- [x] **F1 [P0]** radical-product-squaring-skips-domain-filter (`solve(sqrt(x)*sqrt(x-3)=2, x)`) — CERRADA ciclo 1, commit `36c7dcff1` (filtro post-solve de dominio radical en solve_local_core: re-verifica candidatos Discrete contra la eq radical del nivel + condiciones per-radicando; gate has_radical_product)
- [x] **F2 [P0]** nested-abs-branch-enumeration-fails (`solve(abs(abs(x)-5)=abs(x),x)`) — CERRADA ciclo 2, commit `4baf5821b` (relajar gate de reclamo: reclamar cuando ≥2 abs externos llevan la var; el core de segmentación ya era correcto)
- [x] **F3 [P0]** transcendental-inequality-collapses-to-boundary-equation (`solve(e^x<2^x,x)`) — CERRADA re-ciclo A 2026-07-14, commit `26433e7a2` (handler try_symbolic_linear_coeff_inequality: colecta c1·x+c0, oráculo const-sign, rayo directo vía isolated_var_solution; hooks top-level + solve_inner; 2º intento tras el rechazo del decline)
- [x] **F4 [P0]** poly-rational-root-drops-residual-factor (`solve(x^5 - x^4 - 4*x^3 + 4*x^2 + x - 1 = 0, x)`) — CERRADA re-ciclo B 2026-07-14, commit `71499946d` (Sturm exacto → completeness; residual biquadrático resuelto con signos por Vieta → el representante da las 5 raíces reales; no-resoluble → eco honesto; x^5−1→{1} preservado)
- [x] **F5 [P1]** wrapped-trig-equation-drops-periodic-family (`solve(1/cos(x)^2 = 2, x)`) — CERRADA: recíproco-cuadrado ciclo 6 `393050c8e` + multiple-angle tan re-ciclo C 2026-07-14 `d182fe282` (tan(u)=tan(v) ⟺ u≡v mod π con exclusión exacta de polos por progresiones)
- [ ] **F6 [P0]** unfolded-constant-arithmetic-tree-overflows-simplifier-depth (`[[1,2],[3,4]]^12`) — multi-cycle
- [ ] **F7 [P1]** complex-value-domain-not-plumbed-to-root-emission (`solve(x^3 = 1, x)  [--value-domain complex]`) — multi-cycle
- [~] **F8 [P1]** unsound-sincos-rewrite-of-tan-cot-quotients (`solve(cot(x)^2 - 1 = 0, x)`) — PARCIAL ciclo 5 `94c5bad98` (Layer-1 guard RHS-contains-var) + re-ciclo C 2026-07-14 `d182fe282` (miembro HANG `tan(2x)=tan(3x)` CERRADO→{kπ} instantáneo); facets restantes: `tan/(1-tan²)` polos espurios + Layer-2 periódico cot²

## Cobertura (frentes limpios = 0 confirmados)

**10 de 19 frentes LIMPIOS**, incluyendo TODO el core de cálculo y el frente de regresión:
- LIMPIOS: `inequalities`, `integrals-indef`, `integrals-def`, `limits`, `series`, `number-theory`, `identities-simplify`, `piecewise-special`, `systems`, **`regression`** (los 33 fixes previos INTACTOS — cero regresiones introducidas).
- Con hallazgos: `solve-poly` (8), `radicals` (8), `exp-log` (8), `abs` (8), `transcendental-mixed` (8), `trig-eq` (6), `complex-domain` (6), `matrices` (5), `derivatives` (1).
- Lectura: la frontera de soundness se ha desplazado del cálculo (integrales/límites/series, ahora limpios) al **álgebra de resolución de ecuaciones**: raíces de polinomios, radicales con dominio, desigualdades transcendentales, abs anidado, familias trig envueltas, y el dominio complejo sin cablear.

## Síntesis

The 58 double-confirmed wrong-answers collapse into 8 root-cause families, each a single shared mechanism rather than a topic. A recurring meta-shape unifies most of them: the engine computes the correct constraint / condition / solution-family in ONE place, but a wrapper form or a later stage silently discards it. Three families are near-trivial, high-value chokepoint fixes covering 24 findings, all bounded-single-cycle: (F2) radical equations that square √A·√B→√(AB) and never intersect candidates with the domain conditions they ALREADY emit (extraneous roots survive); (F4) nested absolute-value equations whose inner/outer branch enumeration collapses to an empty set; (F3) transcendental exponential inequalities a^u⋚b^v reduced to their boundary equation with the monotone ray dropped and the excluded boundary point returned. The next tier are soundness gaps in specific solver paths: (F1) bare polynomials that divide out rational roots but drop the residual quartic/sextic factor whole, and (F5, highest breadth at 10) wrapped trig equations (sec²/csc²/1/cos²/reciprocal-tan/multiple-angle) that lose the periodic family the equivalent atomic form already emits correctly. The last three are capability/core work: (F7) an un-folded constant-arithmetic tree that overflows the depth-51 simplifier on matrix powers and high-order derivatives, leaking a mangled/truncated tree or hanging; (F8) a --value-domain complex flag honored in metadata but never propagated to root emission or complex log; (F6) an unsound sin/cos rewrite of tan/cot quotient forms that manufactures self-referential arccos/arcsin garbage trees or spurious poles-as-roots. Best ROI is F2: the fix is a single post-solve filter using conditions the engine has already produced.

## Meta-temas (palanca compartida)

1. **La meta-forma recurrente: el motor CALCULA la condición/familia correcta en UN sitio, y un envoltorio o etapa posterior la descarta en silencio.** F2 emite las `required_conditions` de dominio pero no las intersecta con los candidatos; F5/F8 la forma ATÓMICA (`cos^2=c`, `tan=0`) ya da la familia periódica pero el envoltorio recíproco/cociente la pierde; F3 resuelve la ecuación-borde pero dropea el rayo monótono; F7 el dominio complejo llega al metadata pero no a la emisión de raíces.
2. **Tres P0 son chokepoints casi-triviales (24 hallazgos, bounded-single-cycle):** F2 (filtro post-solve reusando las condiciones YA emitidas), F4 (relajar UN gate de reclamo del handler de abs anidado que YA tiene el core correcto), F3 (hacer op-aware el sink compartido de linear-collect). F1 se cierra soundly como decline honesto (residual grado≥3 → eco).
3. **`--value-domain complex` es metadata sin efecto** (F7): honrado en el bloque `semantics` pero nunca cableado a la emisión de raíces ni al log complejo. Prep de Fase 2 que ya tiene la maquinaria (`complex_support.rs`, `Constant::I`) — falta enhebrar `is_real_only`/`value_domain` una capa más.


### F1 [P0] radical-product-squaring-skips-domain-filter

- **Representante:** `solve(sqrt(x)*sqrt(x-3)=2, x)`
- **Correcto:** { 4 }
- **Miembros (8):** `solve(sqrt(x)*sqrt(x-3)=2, x)`; `solve(sqrt(x)*sqrt(x+3)=2, x)`; `solve(sqrt(x)*sqrt(x-5)=6, x)`; `solve(sqrt(x-1)*sqrt(x-2)=2, x)`; `solve(sqrt(1-x)*sqrt(2-x)=2, x)`; `solve(sqrt(2*x)*sqrt(x-4)=4, x)`; `solve(sqrt(-x)*sqrt(1-x)=2, x)`; `solve(sqrt(x)*sqrt(2*x-6)=6, x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/solve_analysis.rs:1226 (guard_solved_result_with_exclusions — the solve finalization chokepoint that applies only NonZero pole exclusions and never intersects Discrete candidates with the inferred NonNegative/Positive/LowerBound conditions). Recommended concrete fix site: crates/cas_engine/src/api.rs:104 (outermost `solve`).
- **Mecanismo:** `RootMergeMulRule` (crates/cas_engine/src/rules/exponents/rationalization.rs:163) folds √x·√(x-3) → (x·(x-3))^(1/2), widening the real domain from {x≥0 ∧ x-3≥0} to {x(x-3)≥0}. The main pipeline squares the collapsed form to the quadratic x(x-3)=4 → {4,-1} and verifies against the collapsed equation, where √((-1)(-4))=2 passes. Preflight infers the true per-radicand conditions NonNegative(x) and NonNegative(x-3) from the ORIGINAL two-radical LHS (domain_inference.rs:230/442) and stores them in the fork's domain-env `required` set (they appear as the JSON `required_conditions` x≥0, x≥3), but the finalization guard applies only NonZero pole exclusions — the NonNegative/Positive/LowerBound conditions are shown but never used to prune the Discrete candidate set, so x=-1 (which violates both) is kept. Correct in complex mode (inference is real-only), so the family is real-domain-specific.
- **Boceto de fix:** Add a post-solve domain-condition filter that, in the REAL value-domain only, drops any Discrete candidate that PROVABLY violates an inferred required condition. Minimal, lowest-risk placement: at the outer engine solve boundary (cas_engine/src/api.rs::solve, and shared by solve_with_display_steps) — after the backend returns, re-run infer_implicit_domain(ctx, eq.lhs) ∪ infer_implicit_domain(ctx, eq.rhs) on the ORIGINAL (pre-collapse) equation, and for each SolutionSet::Discrete candidate v, substitute var:=v into each NonNegative/Positive/LowerBound condition's expr, simplify, and drop v only when the sign oracle DISPROVES the condition (e.g. NonNegative disproven ⇒ expr provably < 0). Leave Unknown/undecidable candidates untouched (soundness). Alternatively, for a shared cas_solver_core chokepoint, thread the fork's domain-env `required` conditions into the Discrete-result finalization at solve_runtime_flow_orchestration.rs:96 (main-pipeline result, where `ctx` is live) — mirroring apply_nonzero_exclusion_guards_if_any with a new apply_nonnegative/positive filter. Both approaches close all 8 members (e.g. drops -1, -4, -3, and the out-of-range surd roots) and any other squaring path that loses per-radicand domains.
- **Blast radius:** Narrow. Only affects SolutionSet::Discrete results in the real value-domain; a candidate is removed exclusively when an inferred required condition is definitively Disproven at that point, so legitimate roots (which satisfy the conditions) are never dropped. Already-correct paths are unaffected: single-radical filtering (verification) and √A=√B keep their valid roots; complex mode is untouched because infer_implicit_domain returns empty when not real-only. Residual risk is limited to (a) a spuriously inferred condition or (b) the sign oracle mis-Disproving a surd substitution like 1-((3+√17)/2); mitigate by dropping only on a definite Disproven (never on Unknown) and gating on value_domain==Real. No change to steps/didactic contracts beyond a shorter solution set.
- **Maquinaria reutilizable:** infer_implicit_domain (crates/cas_solver_core/src/domain_inference.rs:230; recursive walker at :442 already yields NonNegative for every radicand under a Mul); ImplicitCondition/ImplicitDomain vocabulary and is_trivial sign checks (domain_condition.rs); predicate_proofs::prove_nonnegative_core_with / prove_positive_core_with plus crate::helpers::prove_nonnegative / prove_positive — the exact sign provers RootMergeMulRule already uses to emit these very conditions; surd/const sign decisions via provable_sign_vs_zero / compare_values; substitution + simplify from the existing verification path (verify_substitution::substitute_equation_diff); and apply_nonzero_exclusion_guards_if_any (solve_analysis.rs) as the structural template for a NonNegative/Positive sibling guard.
- **ROI:** BEST ROI. 8 identical failures: √A·√B is collapsed to √(AB), both sides squared to a quadratic, and the extraneous root is kept. The engine ALREADY computes and emits the required_conditions (x>=0, x>=3, etc.) but never intersects the candidate set with them — several outputs literally include a value that violates the engine's own listed condition. Fix = one post-solve filter / back-substitution at the radical-equation chokepoint using conditions already in hand. Near-zero risk, closes all 8 and likely every other squaring-based solver path.

### F2 [P0] nested-abs-branch-enumeration-fails

- **Representante:** `solve(abs(abs(x)-5)=abs(x),x)`
- **Correcto:** { -5/2, 5/2 }
- **Miembros (8):** `solve(abs(abs(x)-5)=abs(x),x)`; `solve(abs(abs(x)-4)=abs(x),x)`; `solve(abs(abs(x)-2)=abs(x-1),x)`; `solve(abs(abs(x-2)-5)=abs(x),x)`; `solve(abs(abs(x)-5)=2*abs(x),x)`; `solve(abs(abs(2*x)-5)=abs(x),x)`; `solve(abs(x)=abs(abs(x)-5),x)`; `solve(abs(abs(x)-5)=abs(x-1),x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:5816
- **Mecanismo:** The nested-abs handler `try_solve_nested_abs_relation` (solve_backend_local.rs:5730, dispatched at line 10040 before every other abs-equation handler) already contains the correct breakpoint/segment enumeration core (5881-5921). But its claim gate at 5811-5818 restricts it to the "variable-remainder" family (`||x|-2| {op} x`): it substitutes 0 for EVERY outermost abs in the difference and declines if the leftover remainder has no variable. For an abs-vs-abs equation like `||x|-5| = |x|`, both sides are abs, so zeroing all outer abs (including the variable-bearing RHS `|x|`) leaves the constant 0 — no variable — and the gate returns None at 5816. The equation then falls through to `try_solve_polynomial_in_abs` / generic isolation, which peel only one abs and enumerate a subset of branches: the empty set for 6 members, half the set for `||2x|-5|=|x|` ({5,-5}), and a mangled positive-branch residual for `|x|=||x|-5|`. The handler's segment core is sound — I hand-verified the regional re-entry sub-equations (|x-5|=x→{5/2}, |x+5|=-x→{-5/2}, |x+3|=|x|→{-3/2}, |x-7|=|x|→{7/2}) all solve correctly, so only the gate blocks the family.
- **Boceto de fix:** Relax the claim gate at solve_backend_local.rs:5816 so `try_solve_nested_abs_relation` also claims the abs-vs-abs / nested-abs-sum shape while still declining the single-outer-abs-vs-constant case owned elsewhere. After collecting `outer_abs` (5808-5809), count how many outermost abs carry the variable and only decline a var-free remainder when fewer than two do:\n```rust\nlet var_outer_abs = outer_abs.iter().filter(|&&a| match simplifier.context.get(a) {\n    Expr::Function(_, args) if args.len() == 1 => contains_var(&simplifier.context, args[0], var),\n    _ => false,\n}).count();\nif !contains_var(&simplifier.context, remainder, var) && var_outer_abs < 2 { return None; }\n```\nThe existing segment core needs no change: breakpoints from the inner abs alone suffice because the outer/RHS abs are resolved exactly by the regional `solve_relation_set` re-entry over R and then clipped to each segment. Hand-verified this yields the correct roots for all 8 members: `||x|-5|=|x|`→{-5/2,5/2}, `||x|-4|=|x|`→{-2,2}, `||x|-2|=|x-1|`→{-1/2,3/2}, `||x-2|-5|=|x|`→{-3/2,7/2}, `||x|-5|=2|x|`→{-5/3,5/3}, `||2x|-5|=|x|`→{-5,-5/3,5/3,5}, `|x|=||x|-5|`→{-5/2,5/2}, `||x|-5|=|x-1|`→{-2,3}.
- **Blast radius:** Additive widening of one handler that runs before all other abs-equation handlers (dispatch line 10040). Newly-claimed forms (equations with a nested affine inner abs AND >=2 variable-bearing outermost abs AND a var-free remainder) currently ALL return wrong answers, so every change is an improvement. Preserved: `||x|-2|=1` and `|||x|-1|-1|=0` keep a single outer abs (count=1) -> still declined -> pinned tests `sibling_abs_owners_are_unchanged` ({3,-3,1,-1}), stress_solve_tests.rs:820, :1037, torture_tests.rs:379 stay green. Inequalities untouched: nested-abs `|f|<|g|` is intercepted earlier at line 10035 (try_solve_abs_vs_abs_polynomial_inequality honest-declines non-poly args), and the `||x|-2|<x` var-remainder case already reaches this handler. The `is_concrete_solution_set` guard at 5914 still forces an honest decline on any unresolved regional solve, so no mangled output. No existing test pins the family's wrong behavior. Recommend running the full cas_solver suite plus the scorecard huella to catch any incidental reordering on nested-abs sums.
- **Maquinaria reutilizable:** Everything needed is already inside try_solve_nested_abs_relation and its callees: the breakpoint-partition loop (5882-5920), per-segment inner-abs sign substitution via arg_polys[i].eval(test) + substitute_expr_by_id (5899-5908), regional re-entry through solve_relation_set (5910) that recursively solves the now-single-level abs relations, segment_set clipping (5859-5879), intersect_solution_sets/union_solution_sets (5918-5919), and the is_concrete_solution_set decline guard (5914). contains_var (cas_solver_core::isolation_utils) and the existing collect_outer_abs closure (5783) supply the new predicate. No new algorithm is introduced — only the gate predicate changes.
- **ROI:** 8 members, all nested ||x|+-a|=|x+-b| forms. Six return the maximally-wrong empty set for a real 2-root equation; one returns half the set ({5,-5}); one returns a mangled positive-branch-only piecewise (dropped the negated branch). Single canonical fix: substitute u=|x|>=0, solve |u-c|=... for u>=0, then enumerate inner/outer abs branches — the split-piecewise-at-abs-zero machinery already exists in the repo. Reuses across every nested-abs equation.

### F3 [P0] transcendental-inequality-collapses-to-boundary-equation

- **Representante:** `solve(e^x<2^x,x)`
- **Correcto:** (-inf, 0)  (open interval, 0 excluded)
- **Miembros (8):** `solve(e^x<2^x,x)`; `solve(e^x>2^x,x)`; `solve(e^x<=2^x,x)`; `solve(2^x>e^x,x)`; `solve(pi^x<2^x,x)`; `solve(e^(2x)<4^x,x)`; `solve(e^x<2*2^x,x)`; `solve(e^(x+1)<2^x,x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/linear_collect.rs:270 (factored) and :152 (additive) — both funnel into crates/cas_solver_core/src/linear_solution.rs:16 build_linear_solution_set, which is op-blind. Invoked op-obliviously at crates/cas_solver_core/src/isolation_arithmetic.rs:80 (add-isolation BothOperands) and :569 (mul-isolation RHS-has-var).
- **Mecanismo:** For a member like solve(e^x<2^x): the base ratio e/2 is NON-numeric, so the numeric-base exponential terminal declines and solve_inner log-linearizes inside the recursive route: e^x {op} 2^x -> x·ln(e) {op} ln(2^x) -> x {op} x·ln(2), i.e. (1-ln2)·x {op} 0 — a linear inequality whose variable now sits on both sides with a SYMBOLIC (transcendental) coefficient. Isolation routes var-on-both-sides through the add/mul-isolation `try_linear_collect` fallback (isolation_arithmetic.rs:80 / :569). That fallback calls the linear-collect factored/additive pipeline, which is EQUATION-ONLY: derive_linear_collect_factored_kernel correctly factors coeff=(1-ln2), rhs_term=0, then build_linear_solution_set (linear_solution.rs:24-26) returns SolutionSet::Discrete([rhs_term/coeff]) = {0}, silently DROPPING the operator — the boundary root as a point, which is spurious for strict ops (equality holds there). Confirmed reproductions: solve(x<x*ln(2))={0}, solve(x*ln(2)<x*ln(3))={0}, solve(3*x<5*x*ln(2))={0}, all 8 members give {0}/[0,0]/singleton-radical. The downstream recovery try_polynomial_inequality_sign_analysis (solve_backend_local.rs:10509/:10570) cannot save it: it uses Polynomial::from_expr which requires RATIONAL coefficients (:10589) so it declines the symbolic (1-ln2); and for the exp inputs the diff e^x-2^x is non-polynomial in x, so from_expr fails outright. Pure integer-base pairs (2^x<3^x) already work because their ratio 2/3 is numeric and takes a different (numeric-base terminal) path — the bug is specific to e/π reductions.
- **Boceto de fix:** Make the linear-collect pipeline op-aware at the single shared sink. Thread the RelOp (already in scope at isolation_arithmetic.rs:263 and :804, and passed to both try_linear_collect closures at :305 and :852) into execute_linear_collect_factored_pipeline_* / _additive_pipeline_*. When op is an inequality (Lt|Leq|Gt|Geq) and the derived coeff is var-free (a genuine constant), instead of build_linear_solution_set emit Mul(coeff, var) {op} rhs_term and dispatch it via the already-available solve_rewritten/isolate callback — the new LHS is a single Mul, so it routes to the EXISTING mul-isolation pipeline (isolation_arithmetic.rs:499/785), which already flips the op when coeff is provably negative via the exact is_known_negative/const-sign oracle and honestly declines an unknown-sign coeff ("Inequalities with symbolic coefficients not yet supported"). For op==Eq keep the current build_linear_solution_set path untouched. Verified that the pre-collected form of every member's coefficient already resolves correctly through mul-isolation: (1-ln2)·x<0->(-inf,0), (ln(pi)-ln2)·x<0->(-inf,0), (2-ln4)·x<0->(-inf,0), (1-ln2)·x<ln2->(-inf,ln2/(1-ln2)), (1-ln2)·x<-1->(-inf,1/(ln2-1)), x·(ln2-1)>0->(-inf,0). So this one change closes all 8 members.
- **Blast radius:** LOW. Strictly gated on inequality ops + var-free coeff, so equation solving (including the parametric degenerate branch a·x=a and solve(x=x*ln(2)) conditional output) is untouched. Rational-coefficient inequalities currently recovered downstream by sign-analysis (2*x<x, 2*x+1<x+3) will now resolve at the core with identical answers (verified x<0, x<2). Unknown-sign symbolic coeffs (a*x<0) keep the existing honest decline. Pure integer-base exponentials take a different path and are unaffected. Sweep needed: linear_collect.rs unit tests all pin RelOp::Eq (inequality behaviour is additive, not changed); and any fixtures pinning the current wrong {0}/[0,0]/singleton output for these transcendental inequalities would be intended updates, not regressions.
- **Maquinaria reutilizable:** derive_linear_collect_factored_kernel (linear_collect.rs:65, already factors coeff·x+rhs including symbolic coeffs); the mul-isolation pipelines with the is_known_negative op-flip (isolation_arithmetic.rs:499+ and :785+) which already solve coeff·x {op} rhs for provably-signed symbolic coeffs and decline unknown-sign honestly; the solve_rewritten/isolate_rewritten recursive callback already passed into both isolation pipelines; SolutionSet::Continuous ray construction. No new solver machinery required — only threading op and one inequality branch.
- **ROI:** 8 members that all reduce uniformly to (c)^x ⋚ 1. Documented 'inequality collapses to its boundary equation, monotone ray dropped' family: the engine solves the boundary a^u=b^v, returns that single root (as {0}, [0,0], or a singleton radical), and for strict inequalities the returned point is itself spurious (equality holds there). Fix = after locating the boundary root, use sign(ln c)/monotonicity to emit the ray instead of the point. One chokepoint, broad reuse across all exp/base inequalities.

### F4 [P0] poly-rational-root-drops-residual-factor

- **Representante:** `solve(x^5 - x^4 - 4*x^3 + 4*x^2 + x - 1 = 0, x)`
- **Correcto:** { 1, sqrt(2-sqrt3), -sqrt(2-sqrt3), sqrt(2+sqrt3), -sqrt(2+sqrt3) } = { 1, +-0.5176380902, +-1.9318516526 }
- **Miembros (8):** `solve(x^5 - x^4 - 4*x^3 + 4*x^2 + x - 1 = 0, x)`; `solve(x^7 - 7*x^5 + 14*x^3 - 7*x - 1 = 0, x)`; `solve(x^5 - 5*x^3 + 5*x - 1 = 0, x)`; `solve(x^5 - 5*x^3 + 5*x + 1 = 0, x)`; `solve(x^5 + 2*x^4 - 2*x - 1 = 0, x)`; `solve(x^5 + 4*x^4 + 2*x^3 - 2*x^2 - 4*x - 1 = 0, x)`; `solve(x^5 + 3*x^4 - 3*x - 1 = 0, x)`; `solve((x-3)*(x-1)*(x^4+3*x^3+3*x^2+3*x+1) = 0, x)`
- **Tractabilidad:** bounded-single-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/rational_roots.rs:792
- **Mecanismo:** In `extract_candidate_roots` (rational_roots.rs:780-797) the rational-roots strategy peels linear factors via `find_rational_roots`, which returns the leftover residual coefficients. The residual is only solved when its degree is <= 2 (the gate `if residual_coeffs.len() == 3 || residual_coeffs.len() == 2` at line 792). A residual of degree >= 3 — e.g. the quotient `x^4-4x^2+1` after peeling x=1 from the representative quintic — is silently DROPPED, and no incompleteness is signalled. The strategy wrapper `solve_rational_roots_strategy_result_for_equation_with_and_item_with_state` (line 499-519) declines (returns None -> honest Residual/echo) ONLY when `roots.is_empty()`; a non-empty-but-partial list like `[1]` is wrapped as a complete `SolutionSet::Discrete([1])`. That masking Discrete result then blocks recovery: every downstream augment/decline handler in solve_backend_local.rs (try_solve_polynomial_with_cubic_factor @10418, try_solve_biquadratic @10432, try_solve_polynomial_with_quartic_factor @10442, the symbolic-poly honest-echo @10475) is gated on set being Residual/Conditional (or, for the quartic augmenter, a strictly-longer Discrete it cannot produce for a biquadratic quotient). Net: a bare quintic with 5 real roots reports {1}; the (x-3)(x-1)(quartic) member reports {1,3}. Confirmed the strategy path is the emitter: min/max degree bounds are [3,10] (DEFAULT_RATIONAL_ROOTS_MIN/MAX_DEGREE), quintic/septic both in range; `x^5-x-1` (empty roots) declines, `x^5-...=1` (partial roots) emits — exactly the empty-vs-partial split at line 512.
- **Boceto de fix:** Make the dropped residual OBSERVABLE and treat "residual not fully reduced" identically to "no roots" (honest decline -> Residual echo), which closes the ENTIRE family soundly in one bounded chokepoint change. Concretely: have `extract_candidate_roots` also report completeness (return `(Vec<ExprId>, bool complete)` where complete = the residual was reduced to degree <= 2 / exhausted to a nonzero constant), thread it through `NumericPolynomialSolveOutcome::CandidateRoots { degree, roots, complete }` (rational_roots.rs:354, produced at 770-771), and in the strategy at line 512 decline (`return None`) whenever `!complete` — not only when `roots.is_empty()`. This turns all 8 members from wrong partial sets into honest echoes (matching the existing `x^5-x-1` behaviour), with NO wrong answers. The two unit tests at rational_roots.rs:1088 and 1103 use degree-<=2 residuals (complete==true) so their root counts are unchanged; only the tuple/field destructure needs updating. GRADED capability ON TOP (optional ROI, plugs into the existing augmenters which already REPLACE a Residual with a complete set): (a) bounded quick win — add a biquadratic-quotient branch: when the deflated quotient has only even powers, substitute u=x^2, solve the quadratic-in-u, back-substitute x=+-sqrt(u) (reuse try_solve_biquadratic @solve_backend_local.rs:10760 and quadratic_formula::sqrt_expr); closes the representative x^4-4x^2+1 and any biquadratic-cofactor member; (b) a general quartic resolvent solver closes members 5-8 (irreducible-into-rational-quadratics all-real quartics); (c) the x^7 Chebyshev sextic (all-real, no radical form) stays an honest decline or needs a trig-form solver.
- **Blast radius:** Isolated to the rational-roots STRATEGY path in cas_solver_core (extract_candidate_roots / solve_numeric_coeff_polynomial / the strategy wrapper's empty-check). The other three `find_rational_roots` consumers are untouched because they read the returned quotient themselves: matrix eigenvalues (cas_engine/matrix_rule_support.rs:525,673) and the backend handlers try_solve_polynomial_with_cubic_factor / try_solve_polynomial_with_quartic_factor (solve_backend_local.rs:10961,11733). Regression risk LOW: fully-deflating polynomials (residual degree <=2, e.g. (x-1)(x-2)(x^2-5)) stay complete==true and are unaffected; pure cubics still route to the Cardano handler (try_solve_polynomial_with_cubic_factor); the rational-quadratic-factorable quintic `x^5-5x^3+x^2-5` still yields {-1,+-sqrt5} — verified working today via try_solve_polynomial_with_quartic_factor, whose Residual->complete branch (line 10444) still fires once the masking Discrete becomes an honest Residual. No test fixtures pin the wrong {1}/{1,3} outputs (ripgrep across crates found none). Only the two deflation unit tests (rational_roots.rs:1088,1103) need the new tuple/field.
- **Maquinaria reutilizable:** `find_rational_roots` already returns the residual coeffs — the completeness signal is present, it just needs surfacing. Augment/decline dispatch already exists and consumes a Residual: try_solve_polynomial_with_cubic_factor (solve_backend_local.rs:11718, Cardano incl. casus irreducibilis), try_solve_polynomial_with_quartic_factor (10942, quartic->two rational quadratics, verifies roots numerically), try_solve_biquadratic (10760, u=x^2 substitution), factor_monic_quartic_into_rational_quadratics (~10900), quadratic_formula::sqrt_expr and roots_from_a_b_delta, and solve_outcome::residual_solution_set for the honest echo. The augment chain at solve_backend_local.rs:10418-10453 already REPLACES a Residual with a complete set, so any new quotient solver plugs in with no extra wiring.
- **ROI:** 8 bare polynomial solves; rational-root testing / linear-factor division works but the residual higher-degree factor is dropped whole, silently emitting a concrete finite set (e.g. {1} for a quintic with 5 real roots). Graded fix: the biquadratic cofactor (representative x^4-4x^2+1) is a bounded quick win via quadratic-in-x^2; a general quartic/resolvent solver closes 7 of 8; the one irreducible all-real Chebyshev sextic (x^7 case) needs a trig-form solver or an honest numeric/decline. High visibility (bare quintic -> {1}).

### F5 [P1] wrapped-trig-equation-drops-periodic-family

- **Representante:** `solve(1/cos(x)^2 = 2, x)`
- **Correcto:** { pi/4 + k*pi/2 : k in Z }  (i.e. {pi/4, 3pi/4, 5pi/4, 7pi/4} per period)
- **Miembros (10):** `solve(tan(2*x)=tan(x), x)`; `solve(tan(2*x) - tan(x) = 0, x)`; `solve(1/(1-tan(x)^2)=1, x)`; `solve(2*tan(x)/(1-tan(x)^2)=tan(x), x)`; `solve(tan(x) = tan(2*x), x)`; `solve(sec(x)^2 - 2 = 0, x)`; `solve(sec(x)^2 = 4, x)`; `solve(csc(x)^2 = 4, x)`; `solve(csc(x)^2 - 2 = 0, x)`; `solve(1/cos(x)^2 = 2, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver/src/solve_backend_local.rs:7956 (the `squared` closure inside `try_solve_periodic_trig_equation_ungated`, lines 7956-7977); secondary gap at :9359 `try_solve_reciprocal_trig_equation` / :9314 `match_reciprocal_trig_call` / the Function-only guard at :9411-9414.
- **Mecanismo:** `sec(x)^2`, `csc(x)^2`, and `1/cos(x)^2` all canonicalize (simplifier folds `cos^-1→sec` but keeps `cos^-2`) to the SAME node `Div(1, Pow(cos|sin(g), 2))`. This shape matches NEITHER family handler: (1) `try_solve_reciprocal_trig_equation` requires `call` to be `Expr::Function` (line 9411-9414 `_ => return None`) and its Sec/Csc arm (9424) only reduces the bare power-1 `sec(g)=c`→`cos(g)=1/c` via `solve_relation_set` (which is why `sec(x)=2` correctly yields the family); the squared `call` is a `Pow`/`Div`, so it returns None. `match_reciprocal_trig_call` only recognizes `c/trig(g)` for a bare `Function` trig, not a `Pow(trig,2)` inside the Div. (2) The `squared` closure in `try_solve_periodic_trig_equation_ungated` peels a rational coefficient then requires `Pow(base, exp==2)` with `base` a bare `Function(Sin|Cos)` (line 7961-7968); the reciprocal shape's core is a `Div`, not a `Pow`, so it never matches — and even if it did, the base gate is `Sin|Cos` only. With both family attach-points declining, the equation falls through to the generic outside-in isolation dispatch, which inverts `Div → Pow(cos,2)=1/c → cos(g)=±√(1/c) → arccos(±) principal`, returning a finite `Discrete` set (`{π/3,2π/3}` for c=4) — dropping the `+kπ` period AND the supplementary within-period branch. (The periodic-aware substitution path `solve_polynomial_in_atom` at :2165 is NOT reached: `1/u^2` fails its degree≥2 polynomial gate.) The 5 tan(2x)/tan²=0 members are a distinct mechanism: the multiple-angle expansion reduces to `t³+t=0`→`tan(x)=0` but the back-substitution returns only the principal `{0}` instead of `{kπ}`.
- **Boceto de fix:** Bounded chokepoint for the reciprocal-square subfamily (5 members): in `try_solve_periodic_trig_equation_ungated`, immediately after the `simplify` at line 7946-7947 (so `sec²`/`csc²`/`1/cos²` are all in canonical `Div` form), add a reciprocal-power reducer. Match `Div(A, Pow(trig(g), n))` (or `Pow(trig(g), -n)`) with `A` a rational const, `trig ∈ {Sin,Cos}`, `n` a positive integer, `g` containing `var`, on whichever side is non-constant; let `k = as_rational_const(other_side)`. If `k == 0` return `SolutionSet::Empty` (LHS is never 0); if `k` is not a rational const, decline (residual); otherwise build `trig(g)^n = A/k` and recurse via `try_solve_periodic_trig_equation(&reduced, var, simplifier)`. The existing `squared` block (n=2) then runs the double-angle reduction `cos(2g)=2c-1` and the odd-power block handles odd n, both emitting the full periodic family. For even n with `A/k ≤ 0` the sin/cos² path already yields Empty (sound, since `cos²≥0`). No spurious roots: `cos(g)²=1/c≠0` forces `cos(g)≠0`, honoring the original `sec` domain. This closes `1/cos(x)^2=2`, `sec(x)^2=4`, `sec(x)^2-2=0`, `csc(x)^2=4`, `csc(x)^2-2=0`. The 5 tan multiple-angle members need a SEPARATE fix (make the tan²=0 / double-angle-tan back-substitution periodic-aware so `tan(x)=0`→`{kπ}` survives) — genuinely a second cycle.
- **Blast radius:** Low for the reciprocal-square reducer. It fires only on `Div(A, Pow(sin|cos(g), n)) = rational` — a shape that TODAY falls through to the generic principal-value isolation (always wrong for these), so it can only replace wrong finite sets with the correct family or a sound Empty. Guards required: k≠0 (else Empty), symbolic/irrational k declines to residual, arg must contain var, and it must run only inside the periodic handler (which already sits after the bare `try_solve_reciprocal_trig_equation`, so power-1 `sec(g)=c` and `c/trig(g)=k` are untouched). No shared node collides: bare `cos(g)^2=c` is a `Pow` not a `Div`, so the new `Div`-matcher won't intercept the already-correct direct path. Regression sweep: re-run existing trig-equation and reciprocal-trig tests; verify `1/sec(x)^2=1/4` (already correct → `cos^2` via simplify) is unaffected.
- **Maquinaria reutilizable:** `try_solve_periodic_trig_equation` (double-angle `sin/cos(g)^2=c → cos(2g)=2c∓1` reducer at :8040-8058 and odd-power reducer at :8089-8129) already emits the full family — the fix just feeds it the rewritten equation. Also reuse `peel_rational_coefficient`, `cas_math::numeric_eval::as_rational_const`, `contains_var`, and `SolutionSet::Empty`. The k==0→Empty and `trig(g)=1/c` reduction pattern already exists in the Sec/Csc arm at :9424-9438 and the `c/trig(g)=k` arm at :9387-9408 (mirror its constant handling). `union_branch_solutions`/`union_periodic_families_over_common_period` (:2207) is the periodic-aware combiner the separate tan fix would lean on.
- **ROI:** Highest breadth (10). Key leverage: the DIRECT equivalents already return the correct periodic family (solve(cos(x)^2=1/2) -> {pi/4+k*pi/2 : k in Z}, solve(tan(x)=0) -> {k*pi}), so only the reciprocal/quotient/multiple-angle wrappers (sec^2, csc^2, 1/cos^2, 1/(1-tan^2), tan(2x)) route around the family-attaching path and collapse to a bare finite set — the tan forms collapse all the way to {0}, and sec^2/csc^2 also drop the supplementary within-period branch. Fix = route wrapped forms through the same reduced-atomic-equation solver that already emits the Periodic set. Documented open 'periodic trig EQUATIONS' gap; multi-cycle but very broad reuse.

### F6 [P0] unfolded-constant-arithmetic-tree-overflows-simplifier-depth

- **Representante:** `[[1,2],[3,4]]^12`
- **Correcto:** [[138067399, 201223170], [301834755, 439902154]]
- **Miembros (6):** `[[1,2],[3,4]]^12`; `[[2,1],[1,1]]^12`; `[[1,1,0],[0,1,1],[0,0,1]]^10`; `[[2,1],[1,1]]^30`; `trace([[1,2],[3,4]]^12)`; `diff(x^50, x, 50)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_engine/src/const_fold/mod.rs:222
- **Mecanismo:** Ground numeric arithmetic is never folded eagerly at construction; numeric collapse is delegated entirely to the recursive simplifier, which is hard-capped at MAX_SIMPLIFY_DEPTH=50 (crates/cas_engine/src/engine/transform/mod.rs:32, bail at :211). The AST builder Context::add / add_raw (crates/cas_ast/src/expression.rs:356, :478) only structurally canonicalizes (flatten+sort Add/Mul) and folds Neg(Number) — it does NOT fold 2+3, 2·3, or 50-1 into a single Number.

Two producers manufacture all-numeric trees deeper than 50 levels:
(1) MATRIX (members 1-5): Matrix::multiply (crates/cas_math/src/matrix.rs:111-134) builds each dot-product entry as a raw accumulator `sum = Add(sum, Mul(a,b))` starting from `num(0)` (the observed `0 + 1 + 2·3 + …` shape). try_matrix_power_expr (crates/cas_engine/src/matrix_rule_support.rs:410-413) checks its all_numeric gate ONCE on the base (line 393-396), then loops `result = result.multiply(&base)` with NO fold/simplify between iterations, so entries nest one Add/Mul level deeper per power. At ^12/^10 the entry tree exceeds depth 50, the simplifier's Combine-Like-Terms rule cycles ("cycle detected") and bails, leaking the mangled unfolded matrix; ^30 blows up in size and times out.
(2) DIFF (member 6): the power rule (crates/cas_math/src/symbolic_differentiation_support.rs:6640-6643) builds the reduced exponent as `Sub(exp, one)` and the coefficient as `mul_pruned(exp, x^(n-1))`, neither folded. `diff(f,x,50)` desugars to 50 nested single-var diffs (crates/cas_engine/src/symbolic_calculus_call_support.rs) with no fold between layers, so the exponent becomes `50-1-1-…-1` (depth 50) and the coefficient a nested product `50·(50-1)·(50-1-1)·…` — depth > 50, same overflow. The *_pruned helpers (symbolic_differentiation_support.rs:37-91) only prune 0/1 identities, not numeric folding.

Note: raising MAX_SIMPLIFY_DEPTH is the WRONG lever — the ^30 tree is genuinely exponential in size and the diff/matrix depth grows with the exponent/order, so a higher cap just moves the cliff and worsens the perf blowup. The fix must prevent the tree from growing (eager fold) or collapse it with a non-recursive path.
- **Boceto de fix:** PRIMARY (shared chokepoint, reuses existing stack-safe machinery): The const_fold module (crates/cas_engine/src/const_fold/mod.rs) is already an ITERATIVE postorder folder built specifically to avoid stack overflow on deep trees — exactly the "pure-arithmetic depth path" this family needs — but its Add/Sub/Div/Mul(numeric) branches (mod.rs:222, :272, :260, :200) only propagate folded children without folding two-Number operands, and the pass is Off by default (crates/cas_cli/src/cli_args.rs:350) and gated on Safe (crates/cas_engine/src/eval/simplify_action.rs:5445). Fix: (a) add fold_add/fold_sub/fold_div/fold_mul_numeric helpers next to the existing fold_neg/fold_pow (crates/cas_engine/src/const_fold/helpers.rs) that collapse two rational Numbers via exact BigRational arithmetic, and call them in try_fold_node's Add/Sub/Div/Mul arms; (b) run this ground-arithmetic normalization unconditionally as an internal post-simplify pass (folding a·b/a+b of two literals is a domain-independent identity, always sound), not gated on the Safe CLI flag. Because the walk is iterative it collapses arbitrarily deep all-numeric subtrees to a single Number, closing all 6 members regardless of the depth-50 recursive cap.

ALTERNATIVE / DE-RISKING (bounded, closes 5/6 with tiny blast radius): in try_matrix_power_expr (matrix_rule_support.rs:410-414), since the all_numeric gate already holds, fold every result-matrix entry to a Number after the multiply loop via cas_math::numeric_eval::as_rational_const (crates/cas_math/src/numeric_eval.rs:23, already used in matrix.rs::rank) — or fold entries inside Matrix::multiply when both operands are numeric. This collapses ^10/^12/^30 and trace-of-power (the trace then sums single Numbers). It does NOT cover diff(x^50); that member still needs the const_fold-extension path (or a closed-form nth-derivative-of-monomial), which is why the general chokepoint is the honest whole-family fix.
- **Blast radius:** PRIMARY fix: turning ground-arithmetic const-folding on by default changes the FINAL form of every expression that currently leaves an unevaluated numeric subtree, so it will churn pinned baselines broadly — especially cas_didactic step-trace fixtures that intentionally display intermediate unfolded arithmetic (e.g. `50-1`, `2·3`), plus any simplifier rule that pattern-matches on a two-Number Add/Mul it now never sees. The const_fold module has a strict documented safety contract ("NEVER calls simplify/expand, no domain assumptions"); numeric ring-op folding stays within that contract (pure ground evaluation), but the default-on decision + full-workspace baseline sweep is the real cost. The deepest-root variant (folding in Context::add at expression.rs:356) is even higher blast radius — it perturbs every construction and all didactic traces — and is the reason the family is tagged 'simplifier-core surgery'; prefer the iterative-const_fold chokepoint over it. ALTERNATIVE matrix-only fix is nearly risk-free: current matrix-power output is garbage, so no correct baseline pins it; only add matrix-power fixtures would change, to the correct values.
- **Maquinaria reutilizable:** crates/cas_engine/src/const_fold/mod.rs::fold_constants — an already-implemented ITERATIVE (explicit-stack) postorder folder with memoization, purpose-built to avoid stack overflow on deep trees; extend its numeric arms rather than writing a new pass. Existing fold helpers to mirror: fold_neg, fold_pow, fold_sqrt, fold_mul_imaginary (crates/cas_engine/src/const_fold/helpers.rs). Exact rational evaluators for the new helpers: cas_math::numeric_eval::as_rational_const / as_rational_const_depth (crates/cas_math/src/numeric_eval.rs:23,29). Budget plumbing (Budget::preset_cli, Operation::ConstFold) already wired at the call site (simplify_action.rs:5446). For the bounded matrix path, as_rational_const is already used by Matrix::rank (matrix.rs:190) and try_matrix_shape_mismatch_undefined (matrix_rule_support.rs:319).
- **ROI:** 6 members (matrix powers ^10/^12/^30, trace of a matrix power, 50th derivative). One shared root cause: no eager integer/constant folding, so the arithmetic tree grows un-folded (0+1+2*3 style factors) until the simplifier's Combine-Like-Terms rule cycles ('cycle detected') and blows the depth=51 budget, leaking a mangled/truncated tree or hanging (^30 times out; diff hits depth_overflow at 51 with uncombined factorial factors). Eager constant folding + a pure-arithmetic depth path has enormous reuse across every deep constant computation, but it is simplifier-core surgery.

### F7 [P1] complex-value-domain-not-plumbed-to-root-emission

- **Representante:** `solve(x^3 = 1, x)  [--value-domain complex]`
- **Correcto:** { 1, -1/2 + (sqrt3/2)i, -1/2 - (sqrt3/2)i }
- **Miembros (6):** `solve(x^2 = -1, x)  [--value-domain complex]`; `solve(x^4 + 1 = 0, x)  [--value-domain complex]`; `solve(x^3 = 8, x)  [--value-domain complex]`; `solve(x^4 = 1, x)  [--value-domain complex]`; `solve(x^3 = 1, x)  [--value-domain complex]`; `ln(-1)  [--value-domain complex]`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/solution_set.rs:529 (primary funnel: `quadratic_numeric_solution`, `delta<0 & op==Eq → SolutionSet::Empty`). Secondary sites: crates/cas_solver_core/src/quadratic_formula.rs:225-233 (symbolic provably-negative delta → Empty) and its caller chain that already carries `is_real_only` but stops threading it (quadratic_strategy.rs:78/114 has it; the numeric solve call at quadratic_strategy.rs:127 → quadratic_formula.rs:236/247 does NOT receive it); ln-of-negative fold at crates/cas_math/src/const_eval.rs (mk_undefined) with NO complex-log capability anywhere.
- **Mecanismo:** Two independent chokepoints, unified by "value_domain never reaches the value/root emitter."

(A) POLYNOMIAL members (5/6: x^2=-1, x^3=1, x^3=8, x^4=1, x^4+1=0). Every one factors into real linear roots × an irreducible quadratic with negative discriminant: x^2=-1→x^2+1(Δ=-4); x^3=1→(x-1)(x^2+x+1)(Δ=-3); x^3=8→(x-2)(x^2+2x+4)(Δ=-12); x^4=1→(x-1)(x+1)(x^2+1)(Δ=-4). The real roots are peeled correctly; the residual quadratic reaches `quadratic_numeric_solution`, which at line 527-529 unconditionally returns `SolutionSet::Empty` for Δ<0. That function takes `(ctx, op, delta, opens_up, r1, r2)` — no value_domain param — so it cannot emit the conjugate pair. Confirmed: `solve(x^2+x+1=0)` → "No solution" in BOTH real and complex. The downstream filters are already complex-safe (solution_contains_nonfinite treats `i`=Constant::I as finite; check_root returns Unknown for non-rationals; drop_non_real_discrete_solutions at solve_backend_local.rs:10393 is already gated on `is_real_only()`), so nothing downstream would re-drop an `i`-root — the roots are simply never built.

(B) ln(-1). Pure capability gap: `ln` of a negative rational folds to `undefined` (const_eval mk_undefined) and there is NO complex principal-log implementation (only a doc comment `ln(-5)=ln(5)+iπ` at complex_support.rs:1525, unimplemented). `ln(-1) --value-domain complex` still returns `undefined` even though the eval simplify DOES receive complex semantics — the fold to iπ just doesn't exist.

Chokepoint-D note: the solve pipeline's internal `simplifier.simplify()` uses `SimplifyOptions::default()` (RealOnly; orchestration.rs LoopConfig default), so even the symbolic ±-root path (`x^2=i`→`{±i^(1/2)}` works, but `x^2=-1`→`±(-1)^(1/2)` would fold to `±undefined`) can't rely on complex folding. The A-fix sidesteps this by building `i·√|Δ|` explicitly.
- **Boceto de fix:** Multi-cycle, two independent bounded fixes.

Cycle 1 (closes x^2=-1, x^3=1, x^3=8, x^4=1 — the quadratic funnel): thread the already-in-scope `is_real_only` one more layer — from quadratic_strategy.rs:127 into `execute_quadratic_coefficient_solve_plan_with_default_numeric_solution_with_state` → `solve_quadratic_coefficient_solve_plan_with_state` → down to `quadratic_numeric_solution` (add param) and the symbolic-delta guard (quadratic_formula.rs:225-233). In the Δ<0 & op==Eq branch, when `!is_real_only`, build the conjugate pair `(-b ± i·√|Δ|)/(2a)` instead of Empty, reusing the existing `sqrt(-n)→i·√n` machinery in cas_math/src/complex_support.rs (try_rewrite_sqrt_negative_expr) and `Constant::I`. Inequalities (Lt/Gt/etc.) with Δ<0 stay unchanged (a real quadratic with no real roots is still an all-ℝ/∅ real question). This single change also handles the factored cubics/quartics because their irreducible quadratic factor is solved through the very same function. x^4+1=0 is the one nested case (biquadratic z^2=-1→z=±i, then x^2=±i) that may need `try_solve_biquadratic` (solve_backend_local.rs:10760) to also thread complex — note `solve(x^2=i)` already yields `{±i^(1/2)}`, so once z=±i is produced the back-substitution can complete.

Cycle 2 (closes ln(-1)): add a complex-log fold for `ln`/`log` of a provably-negative rational under ComplexEnabled → `ln(|x|) + iπ` (principal branch), analogous to the existing `(-1)^(1/2)→i` fold in const_fold_local/folds/pow.rs.

Acceptable honest-decline alternative if full emission is out of scope: at the solve finalize in solve_local_core_inner (where `opts.value_domain` is in scope), when `is_complex_enabled()` and the polynomial's real-root count is provably below its degree (Empty from Δ<0, or a deflated quadratic went Empty), emit a Residual/honest decline instead of the real-filtered set — converts the wrong answers to honest "complex roots not emitted" at one gate.
- **Blast radius:** Cycle 1 is well-contained: `quadratic_numeric_solution` has exactly ONE production caller (quadratic_formula.rs:247) plus 4 unit tests in solution_set.rs (1560-1601) that pass positional args and would need the new param. The Δ<0/op==Eq branch currently ALWAYS returns Empty in real mode, so real-domain behavior is untouched (guard on `!is_real_only`); inequality branches are left alone. Risk: (1) any place expecting a real-only Discrete set that now sees `i`-bearing roots — but downstream filters already tolerate Constant::I (verified), and the real path is unchanged; (2) verification/back-substitution over ℝ (check_root, numeric_islands) returns Unknown for `i` (kept), so no spurious drop. Cycle 2 (complex log) is additive and gated on ComplexEnabled + provably-negative arg, so real-domain `ln(-1)→undefined` is preserved. Regression surface: complex_number_tests.rs, root_log_parity_tests.rs, quadratic contract tests, and the semantics/solver_soundness_guard suites should be swept.
- **Maquinaria reutilizable:** cas_math/src/complex_support.rs: `try_rewrite_sqrt_negative_expr` (sqrt(-n)→i·√n), `try_rewrite_negative_base_half_power_expr`, `extract_gaussian`/`GaussianRational`, and `Constant::I`. cas_solver/src/const_fold_local/folds/pow.rs already folds `(-1)^(1/2)`→`i` gated on value_domain (template for the ln fold). Engine complex rules (cas_engine/src/rules/complex.rs, rules/exponents/power_rules.rs, rules/logarithms/inverse.rs) already read `parent_ctx.value_domain()` — the pattern to mirror. `is_real_only` is ALREADY threaded to quadratic_strategy.rs (line 78/114); only one more hop is needed. The `drop_non_real_discrete_solutions` gate (solve_backend_local.rs:10393) is already complex-aware and needs no change.
- **ROI:** 6 members. --value-domain complex is honored in the JSON semantics block (value_domain=complex) but never reaches the answer path: polynomial roots stay real-filtered (x^2=-1 -> empty, x^3=8 -> {2}, x^4=1 -> {-1,1}) and ln(-1) returns the real-domain 'undefined' sentinel instead of i*pi. Chokepoint = when domain=complex, stop filtering to real and emit the complex roots / principal complex log. Factoring already exists; multi-cycle to wire complex-root extraction and complex log end-to-end. NOT the excusable real-domain complex-omission (complex was explicitly requested).

### F8 [P1] unsound-sincos-rewrite-of-tan-cot-quotients

- **Representante:** `solve(cot(x)^2 - 1 = 0, x)`
- **Correcto:** { pi/4 + k*pi/2 : k in Z }
- **Miembros (4):** `solve(tan(x)/(1-tan(x)^2)=0, x)`; `solve(tan(2*x)=tan(3*x), x)`; `solve(cot(x)^2 = 3, x)`; `solve(cot(x)^2 - 1 = 0, x)`
- **Tractabilidad:** multi-cycle
- **Causa-raíz (file:line):** crates/cas_solver_core/src/isolation_functions.rs:183 (guard to add at the top of the UnaryInvertible arm, ~line 130/159)
- **Mecanismo:** The generic unary-function isolation applies a principal-branch trig inverse (cos->arccos, sin->arcsin) to isolate f(x) on the LHS without checking that the RHS is free of the solve variable. For cot(x)^2=c the simplifier folds cot^2 -> cos^2/sin^2 and reduces to cos(x)=|sin(x)| (or cos(x)=sin(x)*sqrt(3)); the isolation then inverts cos while the RHS still contains x, fabricating the self-referential residual x = arccos(f(x)) which drops periodicity, and for tan(2x)=tan(3x) branches until it hangs (~216s). The existing U4 gate (isolation_functions.rs:142-158) only declines a NON-AFFINE argument; here the argument x IS affine so the gate passes and the bad inversion proceeds. The exponential isolation already has the missing guard (ensure_pow_exponent_rhs_without_variable, solve_outcome.rs:4202 -> 'Cannot isolate exponential: variable appears on both sides'); proof of asymmetry: solve(exp(x)=x) errors cleanly while solve(cos(x)=x) returns solve(x - arccos(x) = 0, x). A separate facet: tan(x)/(1-tan^2)=0 yields {k*pi/2} because the simplifier rewrites tan/(1-tan^2)->(1/2)tan(2x) and solves tan(2x)=0 while dropping the original tan-pole domain (undefined where 1-tan^2=0 and where tan is undefined) - not an arccos tree, so the Layer-1 guard does not affect it.
- **Boceto de fix:** Layer 1 (bounded chokepoint, kills the garbage/hang class family-wide): in the UnaryInvertible{arg} arm of isolation_functions.rs, before the execute_unary_function_isolation call at line 183, add a RHS-contains-var guard mirroring the exponential path -- if cas_ast::collect_variables(ctx, rhs).contains(var) (or isolation_utils::contains_var), return residual_solution_set(ctx, lhs_call, rhs, op, var) (the exact decline pattern already used at lines 152-158 and 174-182). ~4 lines, one site; converts every arccos/arcsin self-referential tree (and the tan(2x)=tan(3x) hang) into an honest operator-preserving residual. Layer 2 (capability, to return CORRECT periodic answers rather than declining): extend find_trig_atom_containing_var (solve_backend_local.rs:3996) to recognize cot/sec/csc so try_solve_polynomial_in_trig (dispatch line 10183, runs before generic isolation) substitutes u=cot(x), solves u^2=c, and back-substitutes cot(x)=+/-sqrt(c) through the existing try_solve_reciprocal_trig_equation (verified correct) with union_branch_solutions combining the pi-families into the pi/2 answer. Separately add a tan(A)=tan(B) => A-B=k*pi reduction for the multiple-angle member, and preserve the tan/cot pole domain in the tan(x)/(1-tan^2) fold.
- **Blast radius:** Layer-1 guard is narrow: it only changes cases f(x)=g(x) where f is a unary-invertible trig and g still contains the variable -- exactly the self-referential-tree/hang cases. Legitimate f(x)=const isolations are untouched (RHS is variable-free there). Homogeneous linear-trig forms (cos(x)=sqrt(3)*sin(x)) are already caught upstream by try_solve_homogeneous_linear_trig and never reach isolation, so they keep their correct answers. Regression risk is low because every affected tree is currently garbage/hang; still, run the trig + isolation + inv-trig contract suites (inv_trig_contract_tests, parametric_trig_equation_tests, quotient_over_sqrt_tests, known_domain_frontier_pairs) since the arccos residual currently appears in some fixtures. Layer-2 atom extension is additive (new None->Some routing before the generic path) with minimal interaction, but must confirm cot survives as a Function node to dispatch (verified: solve(cot(x)=1) already works).
- **Maquinaria reutilizable:** solve_outcome::residual_solution_set (already imported/used in isolation_functions.rs at 156/180); the exponential precedent ensure_pow_exponent_rhs_without_variable (solve_outcome.rs:4202) as the guard model; cas_ast::collect_variables / isolation_utils::contains_var for the RHS check. For correct-answer routing: try_solve_polynomial_in_trig + solve_polynomial_in_trig_from_diff + solve_polynomial_in_atom (back-sub) + union_branch_solutions / union_periodic_families_over_common_period (solve_backend_local.rs), and the existing try_solve_reciprocal_trig_equation (line 9359) which already solves cot(g)=c, csc(g)=c, sec(g)=c to full periodic families (verified cot(x)=+/-1, sqrt(3)).
- **ROI:** 4 members. The tan/cot quotient forms get an unsound sin/cos rewrite that either (a) drops the tan/cot pole domain, manufacturing spurious poles-as-roots (tan(x)/(1-tan^2)=0 -> {k*pi/2}, half of which the LHS is undefined at), or (b) produces a self-referential garbage tree solve(x - arccos/arcsin(f(sin,cos)) = 0, x) that drops periodicity and branches, sometimes hanging (~216s for tan(2x)=tan(3x)). Fix = suppress the arc-substitution and route cot^2=c and multiple-angle forms to the clean reduced-equation path (shares infra with the wrapped-trig family). Narrow but produces the worst output class (garbage/hang).

## Confirmados por frente (para trazabilidad)


**abs** (8):
- `solve(abs(abs(x)-5)=abs(x),x)` → engine `{"ok":true,"input":"solve(abs(abs(x)-5)=abs(x),x)","result":"No solution","resul` | correcto: x = -5/2 or x = 5/2  (solution set {-5/2, 5/2})
- `solve(abs(abs(x)-4)=abs(x),x)` → engine `No solution (result_latex: \emptyset)` | correcto: x ∈ {-2, 2}
- `solve(abs(abs(x)-2)=abs(x-1),x)` → engine `"No solution" (result_latex \emptyset, ok:true) — a definitive empty-set claim, ` | correcto: x = -1/2 and x = 3/2 (solution set {-1/2, 3/2})
- `solve(abs(abs(x-2)-5)=abs(x),x)` → engine `{"ok":true,"result":"No solution","result_latex":"\\emptyset"} — the engine retu` | correcto: x ∈ {-3/2, 7/2}  (i.e. x = -1.5 and x = 3.5)
- `solve(abs(abs(x)-5)=2*abs(x),x)` → engine `No solution (result_latex: \emptyset); ok:true, value_domain: real` | correcto: {-5/3, 5/3}
- `solve(abs(abs(2*x)-5)=abs(x),x)` → engine `{ 5, -5 } if |x| >= 0` | correcto: { -5, -5/3, 5/3, 5 }
- `solve(abs(x)=abs(abs(x)-5),x)` → engine `solve(x - ||x| - 5| = 0, x) if ||x| - 5| >= 0` | correcto: x = -5/2 or x = 5/2  (i.e. the solution set {-5/2, 5/2})
- `solve(abs(abs(x)-5)=abs(x-1),x)` → engine `"result": "No solution", "result_latex": "\\emptyset" (ok=true, real/generic dom` | correcto: {-2, 3}

**complex-domain** (6):
- `solve(x^2 = -1, x)` [--value-domain complex] → engine `Result: "No solution" (result_latex "\\emptyset"), with semantics.value_domain =` | correcto: x = i or x = -i  (the solution set is {i, -i})
- `solve(x^4 + 1 = 0, x)` [--value-domain complex] → engine `{"result": "No solution", "result_latex": "\\emptyset", "ok": true, "semantics":` | correcto: x ∈ {√2/2 + (√2/2)i, √2/2 − (√2/2)i, −√2/2 + (√2/2)i, −√2/2 − (√2/2)i} — the four fourth roots of −1, i.e. e^(iπ/4), e^(
- `solve(x^3 = 8, x)` [--value-domain complex] → engine `{ 2 }` | correcto: { 2, -1 + i*sqrt(3), -1 - i*sqrt(3) }  (the three complex cube roots of 8; each cubes to 8)
- `solve(x^4 = 1, x)` [--value-domain complex] → engine `{ -1, 1 }` | correcto: { -1, 1, i, -i }  (the four fourth-roots of unity: 1, -1, i, -i)
- `solve(x^3 = 1, x)` [--value-domain complex] → engine `{ 1 }  (JSON result: "{ 1 }"; semantics.value_domain = "complex")` | correcto: { 1, -1/2 - (sqrt(3)/2)i, -1/2 + (sqrt(3)/2)i } — the three cube roots of unity. All three verified by substitution: eac
- `ln(-1)` [--value-domain complex] → engine `undefined` | correcto: i*pi (principal value ≈ 3.14159265i). Verified: exp(i*pi) = -1, and sympy log(-1) = I*pi.

**derivatives** (1):
- `diff(x^50, x, 50)` → engine `A 1195-char, 563-node uncombined/mangled tree instead of the constant: 226295336` | correcto: 30414093201713378043612608166064768844377641568960512000000000000  (= 50!)

**exp-log** (8):
- `solve(e^x<2^x,x)` → engine `{ 0 }  (result_latex: \left\{ 0 \right\}) — a discrete set containing only the p` | correcto: (-inf, 0), i.e. x < 0 (open interval, 0 excluded)
- `solve(e^x>2^x,x)` → engine `{ 0 }  (result_latex: \left\{ 0 \right\}); ok=true, real/generic domain` | correcto: (0, +∞)  — i.e. Interval.open(0, oo), all real x > 0
- `solve(e^x<=2^x,x)` → engine `[0, 0]` | correcto: x ∈ (-∞, 0]  (i.e. x ≤ 0, closed at 0 since e^0 = 2^0 = 1)
- `solve(2^x>e^x,x)` → engine `{ 0 }  (JSON result field: "{ 0 }", result_latex "\\left\\{ 0 \\right\\}")` | correcto: (-∞, 0), i.e. x < 0. Equivalently the open interval from negative infinity to 0 (0 excluded).
- `solve(pi^x<2^x,x)` → engine `{ 0 } (result_latex: \left\{ 0 \right\}), deterministic across re-runs.` | correcto: (-∞, 0), i.e. x < 0. Since pi^x < 2^x ⟺ (pi/2)^x < 1 and pi/2 > 1, the inequality holds exactly for x < 0. sympy gives I
- `solve(e^(2x)<4^x,x)` → engine `{ 0 }  (result field = "{ 0 }", result_latex = "\left\{ 0 \right\}", value_domai` | correcto: (-∞, 0), i.e., x < 0. Equivalently the open interval Interval.open(-oo, 0).
- `solve(e^x<2*2^x,x)` → engine `{ ln(2) / (-ln(2) + 1) }` | correcto: (-∞, ln(2)/(1−ln(2))), an OPEN interval ≈ (-∞, 2.2589). Derivation: e^x < 2·2^x = 2^(x+1); take ln: x < (x+1)ln(2) ⇒ x(1
- `solve(e^(x+1)<2^x,x)` → engine `{ 1 / (ln(2) - 1) }` | correcto: x < 1/(ln(2) - 1), i.e. the open interval (-∞, 1/(ln(2)-1)) ≈ (-∞, -3.2588913533). Derivation: e^(x+1) < 2^x ⟺ x+1 < x·l

**matrices** (5):
- `[[1,2],[3,4]]^12` → engine `ok:true, but result is a 76,835-char (truncated) unsimplified nested tree: "[[6·` | correcto: [[138067399, 201223170], [301834755, 439902154]]
- `[[2,1],[1,1]]^12` → engine `ok:true, but result is a mangled/uncombined expression tree. result field = "[[7` | correcto: [[75025, 46368], [46368, 28657]]
- `[[1,1,0],[0,1,1],[0,0,1]]^10` → engine `result field = "[[1, 10, 2·(0 + 1 + 0 + 1 + 1 + 0) + 39], [0, 1, 10], [0, 2·((((` | correcto: [[1, 10, 45], [0, 1, 10], [0, 0, 1]]
- `[[2,1],[1,1]]^30` → engine `Hang / timeout with no output. `target/release/cas_cli eval "[[2,1],[1,1]]^30" -` | correcto: [[2504730781961, 1548008755920], [1548008755920, 956722026041]]
- `trace([[1,2],[3,4]]^12)` → engine `ok=true, but result is a 45517-character TRUNCATED, uncombined nested-arithmetic` | correcto: 577969553

**radicals** (8):
- `solve(sqrt(x)*sqrt(x-3)=2, x)` → engine `{ -1, 4 }` | correcto: {4}
- `solve(sqrt(x)*sqrt(x+3)=2, x)` → engine `{ -4, 1 }  (with required condition x ≥ 0, value_domain=real, domain mode generi` | correcto: { 1 }  (x = 1)
- `solve(sqrt(x)*sqrt(x-5)=6, x)` → engine `{ -4, 9 }  (result set), with required_conditions x ≥ 0 and x ≥ 5, value_domain:` | correcto: { 9 }  (x = 9 is the unique real solution; the domain requires x ≥ 5)
- `solve(sqrt(x-1)*sqrt(x-2)=2, x)` → engine `{ 1/2·(3 - sqrt(17)), 1/2·(sqrt(17) + 3) }  (with required conditions x ≥ 1, x ≥` | correcto: { (3 + sqrt(17))/2 }  ≈  { 3.5616 }  — a single real solution.
- `solve(sqrt(1-x)*sqrt(2-x)=2, x)` → engine `result: "{ 1/2·(3 - sqrt(17)), 1/2·(sqrt(17) + 3) }"  i.e. { (3-sqrt(17))/2 ≈ -0` | correcto: x = (3 - sqrt(17))/2 = 3/2 - sqrt(17)/2 ≈ -0.5616. This is the only real solution. Substitution: 1-x = (sqrt(17)-1)/2 ≈ 
- `solve(sqrt(2*x)*sqrt(x-4)=4, x)` → engine `{ 2·(1 - sqrt(3)), 2·(sqrt(3) + 1) }  (i.e. { 2 - 2·sqrt(3) ≈ -1.4641, 2 + 2·sqr` | correcto: { 2 + 2·sqrt(3) }  (i.e. 2·(sqrt(3)+1) ≈ 5.4641; the only real solution)
- `solve(sqrt(-x)*sqrt(1-x)=2, x)` → engine `{ "result": "{ 1/2·(1 - sqrt(17)), 1/2·(sqrt(17) + 1) }", required_conditions: N` | correcto: x = (1 - √17)/2 ≈ -1.5616  (solution set { (1 - √17)/2 }). This is the only real solution: the domain requires -x ≥ 0 an
- `solve(sqrt(x)*sqrt(2*x-6)=6, x)` → engine `{ -3, 6 }  (JSON result: "{ -3, 6 }"; required_conditions listed: x ≥ 0 and x ≥ ` | correcto: { 6 }  (x = 6 is the only real solution; x = -3 is extraneous)

**solve-poly** (8):
- `solve(x^5 - x^4 - 4*x^3 + 4*x^2 + x - 1 = 0, x)` → engine `{ 1 }  (JSON: "result": "{ 1 }", ok=true, value_domain=real, domain mode=generic` | correcto: { 1, √(2−√3), −√(2−√3), √(2+√3), −√(2+√3) } = { 1, ±0.5176380902, ±1.9318516526 }. The polynomial factors as (x−1)(x⁴−4x
- `solve(x^7 - 7*x^5 + 14*x^3 - 7*x - 1 = 0, x)` → engine `{ 1 }` | correcto: The polynomial factors as (x - 1)(x^6 + x^5 - 6x^4 - 6x^3 + 8x^2 + 8x + 1), where the sextic is irreducible over Q. It i
- `solve(x^5 - 5*x^3 + 5*x - 1 = 0, x)` → engine `{"schema_version":1,"ok":true,"input":"solve(x^5 - 5*x^3 + 5*x - 1 = 0, x)","res` | correcto: The equation x^5 - 5x^3 + 5x - 1 = 0 has 5 distinct REAL roots: x = 1, and x = 2cos(π/15) ≈ 1.9562952015, x = 2cos(7π/15
- `solve(x^5 - 5*x^3 + 5*x + 1 = 0, x)` → engine `{"schema_version":1,"ok":true,"input":"solve(x^5 - 5*x^3 + 5*x + 1 = 0, x)","res` | correcto: The quintic x^5 - 5x^3 + 5x + 1 = 0 factors as (x+1)(x^4 - x^3 - 4x^2 + 4x + 1) = 0 and has FIVE real roots (all verifie
- `solve(x^5 + 2*x^4 - 2*x - 1 = 0, x)` → engine `{ "result": "{ 1 }", "ok": true, value_domain: real, domain_mode: generic } — en` | correcto: Real domain: three roots. { 1, -3/4 - sqrt(5)/4 + sqrt(2)*sqrt(3*sqrt(5)-1)/4, -3/4 - sqrt(5)/4 - sqrt(2)*sqrt(3*sqrt(5)
- `solve(x^5 + 4*x^4 + 2*x^3 - 2*x^2 - 4*x - 1 = 0, x)` → engine `{ 1 }  (JSON: "result": "{ 1 }", ok: true, value_domain: real)` | correcto: Three real roots: x = 1; x = -5/4 - sqrt(5)/4 + sqrt(2)*sqrt(7 + 5*sqrt(5))/4 ≈ -0.301521559576763; x = -5/4 - sqrt(5)/4
- `solve(x^5 + 3*x^4 - 3*x - 1 = 0, x)` → engine `{ 1 }  (JSON: "result": "{ 1 }", "result_latex": "\\left\\{ 1 \\right\\}", ok:tr` | correcto: The real solution set has THREE elements: { -3.090657850852245, -0.3235557115208503, 1 }. Exactly: x = 1, x = -1 - √2/2 
- `solve((x-3)*(x-1)*(x^4+3*x^3+3*x^2+3*x+1) = 0, x)` → engine `{ 1, 3 }` | correcto: { 1, 3, -3/4 - sqrt(5)/4 + sqrt(2)*sqrt(-1 + 3*sqrt(5))/4, -3/4 - sqrt(5)/4 - sqrt(2)*sqrt(-1 + 3*sqrt(5))/4 } = { 1, 3,

**transcendental-mixed** (8):
- `solve(sec(x)^2 - 2 = 0, x)` → engine `{ 1/4·pi, 3/4·pi }` | correcto: x = π/4 + n·π/2 for all integers n, i.e. { π/4 + k·π/2 : k ∈ ℤ } (equivalently the four families π/4, 3π/4, 5π/4, 7π/4 m
- `solve(sec(x)^2 = 4, x)` → engine `{ 1/3·pi, 2/3·pi }  (result_latex: \left\{ \frac{\pi}{3}, \frac{2}{3}\cdot \pi \` | correcto: sec(x)^2 = 4  ⇒  cos(x)^2 = 1/4  ⇒  cos(x) = ±1/2. Real solution set is the infinite periodic family x = π/3 + kπ  ∪  x 
- `solve(csc(x)^2 = 4, x)` → engine `{ 1/6·pi, -1/6·pi }  (i.e. {π/6, -π/6}); result_latex: \left\{ \frac{\pi}{6}, -\` | correcto: x = nπ ± π/6, n ∈ ℤ. Equivalently the four base families {π/6, 5π/6, 7π/6, 11π/6} + 2πn (from sin(x) = ±1/2). All four b
- `solve(csc(x)^2 - 2 = 0, x)` → engine `{ 1/4·pi, -1/4·pi }   (i.e. the finite set {π/4, −π/4}; result_truncated=false, ` | correcto: x = π/4 + kπ/2, k ∈ ℤ. Equivalently the union of {π/4, 3π/4, 5π/4, 7π/4} + 2kπ. This comes from csc²x = 2 ⟹ sin²x = 1/2 
- `solve(1/cos(x)^2 = 2, x)` → engine `{ 1/4·pi, 3/4·pi }  (bare finite set of two elements, no periodicity annotation;` | correcto: x = π/4 + k·π/2, k ∈ ℤ  (equivalently, within [0,2π): {π/4, 3π/4, 5π/4, 7π/4}). This is the solution set of 1/cos(x)²=2 
- `solve(tan(x) = tan(2*x), x)` → engine `result: "{ 0 }" (result_latex \left\{ 0 \right\}), with required_condition "cos(` | correcto: x = nπ, n ∈ ℤ  (equivalently {nπ}: ..., −π, 0, π, 2π, ...). Derivation: tan(x)=tan(2x) ⇔ 2x ≡ x (mod π) ⇔ x = nπ; algebr
- `solve(cot(x)^2 = 3, x)` → engine `solve(x - arccos(sin(x)·sqrt(3)) = 0, x)  (ok: true, returned as Result; result_` | correcto: x = pi/6 + n*pi  OR  x = 5*pi/6 + n*pi, for n in Z (equivalently x = ±pi/6 + n*pi; within [0,pi): {pi/6, 5*pi/6}). Deriv
- `solve(cot(x)^2 - 1 = 0, x)` → engine `solve(x - arccos(|sin(x)|) = 0, x) if (sin(x)^2)^(1 / 2) >= 0` | correcto: x = π/4 + nπ/2, n ∈ ℤ. Equivalently the union {π/4 + nπ, n∈ℤ} ∪ {3π/4 + nπ, n∈ℤ}. (cot(x)²−1=0 ⟺ cot(x)=±1; cot=1 at π/4

**trig-eq** (6):
- `solve(tan(2*x)=tan(x), x)` → engine `{ 0 }  (with required condition 2·cos(x)^2 − 1 ≠ 0). result="{ 0 }", ok=true, va` | correcto: x = nπ, n ∈ ℤ  (all integer multiples of π: {…, −2π, −π, 0, π, 2π, …}). Equivalently Union of {2nπ} and {2nπ+π}. Domain 
- `solve(tan(2*x) - tan(x) = 0, x)` → engine `{ 0 }  (JSON result: "{ 0 }", result_latex "\left\{ 0 \right\}"; ok=true, real/g` | correcto: x = nπ, n ∈ ℤ (all integer multiples of π): {…, −2π, −π, 0, π, 2π, …}. Equivalently sympy's Union({2nπ}, {2nπ+π}). Deriv
- `solve(1/(1-tan(x)^2)=1, x)` → engine `{ 0 }` | correcto: x = nπ, n ∈ ℤ  (i.e. { k·pi : k ∈ ℤ }). The equation 1/(1−tan²x)=1 forces 1−tan²x=1 ⇒ tan²x=0 ⇒ tan(x)=0 ⇒ x=nπ. The req
- `solve(2*tan(x)/(1-tan(x)^2)=tan(x), x)` → engine `{ 0 }  (result_latex "\left\{ 0 \right\}"; required_conditions: 1−tan(x)≠0, tan(` | correcto: x = nπ, n ∈ ℤ  (i.e. {..., −2π, −π, 0, π, 2π, ...}). The LHS is tan(2x), so tan(2x)=tan(x) ⇒ with t=tan x, 2t/(1−t²)=t ⇒
- `solve(tan(x)/(1-tan(x)^2)=0, x)` → engine `{ k·1/2·pi : k ∈ ℤ }  (LaTeX \left\{ k\frac{\pi}{2} : k \in \mathbb{Z} \right\})` | correcto: { x = n·π : n ∈ ℤ }  (equivalently { k·π : k ∈ ℤ }). The only zeros are where the numerator tan(x)=0, i.e. x=nπ; there t
- `solve(tan(2*x)=tan(3*x), x)` → engine `<no result: runs > 45s, killed by timeout>` | correcto: x = k·π, k ∈ ℤ  (i.e., { k·pi : k ∈ ℤ }). Derivation: tan(2x)=tan(3x) ⟺ 2x = 3x + nπ ⟺ x = -nπ = kπ. All x=kπ give tan(2
