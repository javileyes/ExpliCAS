Tests Ignorados - Lista Exhaustiva

1. 
cli_json_contract_tests.rs
 (8 tests)
Razón: Usan cargo run internamente, lo que causa lock contention durante CI paralelo.

bash
# Ejecutar después de compilar con --release
cargo build -p cas_cli --release
cargo test -p cas_cli --test cli_json_contract_tests -- --ignored

Tests específicos:

test_eval_json_success
test_eval_json_success_has_budget
test_eval_json_parse_error_has_kind_code
test_eval_json_error_kind_in_known_set
test_eval_json_error_code_starts_with_e
test_eval_json_no_hold_in_result
test_eval_json_no_hold_in_error
test_eval_json_schema_version_is_1

SOLUCIONADO!!!
2. 
stress_solve_tests_multivariable.rs
 (1 test)
Razón: Stack overflow - recursión infinita al resolver 
(a/b)^x = c/d
.

SOLUCIONADO!!!
bash
# NO EJECUTAR - causa stack overflow
cargo test -p cas_cli test_nested_fraction_with_powers -- --ignored

3. gcd_exact_tests.rs (2 tests)

Razón: Tests de budget, solo para debugging específico.

bash
cargo test -p cas_engine --test gcd_exact_tests -- --ignored

4. stress_test.rs (1 test)

Razón: Test de detección de overflow, ejecución manual.

bash
cargo test -p cas_engine --test stress_test test_batch_overflow_finder -- --ignored --nocapture
5. substitute.rs (1 test unitario)
Razón: Feature "Engine Pro" - requiere identidades trig automáticas.

bash
cargo test -p cas_engine test_substitute --  --ignored

Comando para ejecutar TODOS los tests ignorados (excepto stack overflow)

bash
# Primero compilar
cargo build --release
# Luego ejecutar todos los ignorados (excluyendo el que causa stack overflow)
cargo test -- --ignored 2>&1 | grep -v "test_nested_fraction_with_powers"