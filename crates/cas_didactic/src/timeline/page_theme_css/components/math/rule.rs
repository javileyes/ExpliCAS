pub(super) const RULE_BOX_CSS: &str = r#"
        .rule-box {
            margin-top: 12px;
            padding: 10px 12px;
            background: var(--rule-bg);
            color: var(--rule-color);
            border-left: 4px solid var(--rule-border);
            border-radius: 8px;
            font-size: 0.95em;
        }
"#;
