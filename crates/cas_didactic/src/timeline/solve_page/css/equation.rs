pub(super) const EQUATION_CSS: &str = r#"
    .equation {
        background: var(--equation-bg);
        padding: 15px;
        border-radius: 8px;
        text-align: center;
        font-size: 1.2em;
        transition: background 0.3s ease;
        overflow-x: auto;
        max-width: 100%;
    }
"#;
