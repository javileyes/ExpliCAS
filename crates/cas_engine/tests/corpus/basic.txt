# Golden Corpus - Basic Commands
# Each line is a command to execute. Lines starting with # are comments.
# The test ensures: (1) no panic, (2) stable output kind

# === Arithmetic ===
2 + 3
10 - 4
3 * 5
15 / 3
2^10
sqrt(16)
sqrt(2)

# === Algebra: Simplify ===
x + x
2*x + 3*x
x^2 + x^2
(x + 1)*(x - 1)
x^2 - 1
(a + b)^2

# === Algebra: Expand ===
expand (x + 1)^2
expand (x + y)*(x - y)
expand (a + b)^3

# === Algebra: Factor ===
factor x^2 - 1
factor x^2 + 2*x + 1
factor x^3 - x

# === Fractions ===
1/2 + 1/3
x/2 + x/3
(x + 1)/(x - 1) * (x - 1)/(x + 2)

# === Roots and Powers ===
sqrt(x^2)
x^(1/2) * x^(1/2)
(x^2)^(1/2)
8^(1/3)

# === Logarithms ===
ln(e)
ln(e^2)
log(10, 100)
ln(x*y)

# === Trigonometry ===
sin(0)
cos(0)
tan(pi/4)
sin(pi/2)
sin(x)^2 + cos(x)^2

# === Derivatives ===
diff x^2, x
diff sin(x), x
diff ln(x), x
diff x^3 + 2*x^2, x

# === Integrals ===
integrate x, x
integrate x^2, x
integrate sin(x), x

# === Solve: Linear ===
solve x + 2 = 5, x
solve 2*x = 10, x
solve 3*x + 1 = 7, x

# === Solve: Quadratic ===
solve x^2 = 4, x
solve x^2 - 5*x + 6 = 0, x
solve x^2 + 1 = 0, x

# === Solve: Exponential/Log ===
solve e^x = 2, x
solve ln(x) = 1, x

# === Solve: Trigonometric ===
solve sin(x) = 0, x
solve cos(x) = 1, x

# === Edge Cases ===
0/1
1/0
0^0
x/x
0*x
x - x

# === Complex ===
i^2
(1 + i)*(1 - i)
sqrt(-1)

# === Session References ===
# (These test session handling)
2*x + 3*x

# === Limits (if supported) ===
limit x, x, 0
limit 1/x, x, inf

# === Equations (exercise __eq__ display) ===
solve x + y = z, x
solve a*x + b = c, x
solve x^2 + y = z, x

# === Hold (user-facing barrier, must remain visible) ===
hold(1*x)
hold(x + 0) + 0
hold(2 * 3)
hold(sin(0))
